# اصلاح مهم Scope ماژول موکل‌ها

## جداسازی پنل وکیل و پنل موکل

این پروژه مربوط به **داشبورد وکیل** است.

پنل مدیریتی خود موکل یک محصول / بخش مستقل است و توسط تیم یا توسعه‌دهنده دیگری در حال توسعه است.

بنابراین در این فاز:

* فقط تجربه و مدیریت موکل از دید **وکیل** طراحی شود.
* هیچ UI یا قابلیت مخصوص پنل موکل طراحی نشود.
* هیچ داشبوردی برای موکل ایجاد نشود.
* هیچ مسیر یا صفحه‌ای برای Login موکل ایجاد نشود.
* هیچ تنظیمات حساب کاربری موکل ایجاد نشود.
* هیچ Notification یا Messaging مخصوص موکل ایجاد نشود.
* هیچ فرضی درباره UI پنل موکل ایجاد نشود.

## Client به عنوان موجودیت مشترک

با وجود جدا بودن پنل‌ها، `Client` باید از نظر Domain یک موجودیت مستقل و قابل شناسایی باشد.

در نسخه فعلی Frontend:

```text
Client
├── id
├── name
├── phone
├── email
├── ...
```

هر Client باید یک شناسه یکتا داشته باشد.

در حال حاضر اطلاعات با Local Storage مدیریت می‌شوند، اما معماری Frontend باید طوری باشد که در آینده بتوان همین Client را از طریق Backend مشترک میان:

```text
Lawyer Dashboard
```

و

```text
Client Dashboard
```

استفاده کرد.

### نکته مهم

در این فاز هیچ Sync واقعی بین دو پنل ایجاد نکن.

فقط ساختار داده و Service Layer را طوری طراحی کن که اتصال آینده به Backend بدون بازنویسی UI امکان‌پذیر باشد.

---

## Scope اطلاعات Client در پنل وکیل

اطلاعاتی که وکیل می‌تواند مدیریت کند شامل موارد موردنیاز محصول است، مانند:

* نام
* شماره موبایل
* ایمیل
* اطلاعات شناسایی در صورت نیاز
* توضیحات داخلی
* پرونده‌های مرتبط
* ضمائم تحت مدیریت وکیل

اما درباره اطلاعات زیر هیچ تصمیمی خارج از Scope این فاز نگیر:

* تنظیمات حساب موکل
* اطلاعات خصوصی مخصوص پنل موکل
* اعلان‌های موکل
* پیام‌های موکل
* پرداخت‌های سمت موکل
* دسترسی موکل به پرونده
* قابلیت‌های پنل موکل

این موارد متعلق به پنل مستقل موکل هستند.

---

## اصل معماری

پنل وکیل و پنل موکل باید در آینده از یک Domain Model مشترک استفاده کنند، اما UI، Permission و تجربه کاربری آنها مستقل باشد.

```text
             Backend / Domain
                    │
          ┌─────────┴─────────┐
          │                   │
   Lawyer Dashboard      Client Dashboard
          │                   │
    Lawyer Perspective    Client Perspective
```

در این فاز فقط سمت **Lawyer Dashboard** را پیاده‌سازی کن.



# Phase 3 — پیاده‌سازی فرانت‌اند ماژول «موکل‌ها»

## هدف این فاز

در این فاز باید ماژول **مدیریت موکل‌ها** برای داشبورد وکیل طراحی و پیاده‌سازی شود.

این ماژول باید فراتر از یک جدول ساده باشد و به‌عنوان یک بخش واقعی از محصول مدیریت پرونده‌های حقوقی عمل کند.

### محدودیت‌های فعلی پروژه

در این فاز:

* Backend نداریم.
* Database نداریم.
* Prisma نداریم.
* API واقعی نداریم.
* Storage واقعی نداریم.
* پرداخت واقعی نداریم.
* احراز هویت سمت سرور نداریم.
* داده‌ها فعلاً در Frontend مدیریت می‌شوند.
* ماندگاری داده‌ها با `localStorage` انجام می‌شود.
* سیستم احراز هویت فعلی پروژه باید حفظ شود.

این ماژول باید طوری پیاده‌سازی شود که در آینده بتوان Local Storage را با API واقعی جایگزین کرد، بدون اینکه UI و منطق اصلی محصول نیاز به بازنویسی اساسی داشته باشد.

---

# 1. اصل مهم: موکل یک موجودیت مستقل است

موکل نباید صرفاً اطلاعاتی باشد که داخل پرونده ذخیره شده است.

ساختار مفهومی:

```text
Client
│
├── اطلاعات شخصی / تماس
│
├── Attachments
│
└── Cases
      ├── Case 1
      ├── Case 2
      └── Case 3
```

یک موکل می‌تواند در آینده چند پرونده داشته باشد.

بنابراین از ایجاد اطلاعات تکراری برای یک موکل جلوگیری کن.

اگر در ماژول «پرونده‌ها» قبلاً Client ایجاد شده است، همان Client باید در این ماژول قابل مشاهده باشد.

**نباید برای ماژول پرونده‌ها و ماژول موکل‌ها دو منبع داده جداگانه ایجاد شود.**

---

# 2. بررسی معماری فعلی قبل از کدنویسی

قبل از هر تغییر:

* ساختار فعلی پروژه را بررسی کن.
* ماژول `cases` را بررسی کن.
* نحوه ذخیره Client در فاز قبلی را بررسی کن.
* Store / Service مربوط به Client را پیدا کن.
* نحوه استفاده از Local Storage را بررسی کن.
* کامپوننت‌های قابل استفاده مجدد را پیدا کن.
* الگوهای UI موجود در Dashboard را بررسی کن.

اگر در فاز قبلی برای Client ساختار مناسبی وجود دارد، **آن را مجدداً استفاده و توسعه بده** و ساختار موازی ایجاد نکن.

در این مرحله هنوز هیچ کدی را تغییر نده.

پس از بررسی معماری، گزارش بده و منتظر تأیید بمان.

---

# 3. ساختار صفحه

مسیر اصلی:

```text
/admin/clients
```

در Sidebar داشبورد، گزینه:

**موکل‌ها**

اضافه یا فعال شود.

Icon باید با سایر آیتم‌های Sidebar هماهنگ باشد.

---

# 4. صفحه اصلی موکل‌ها

صفحه `/admin/clients` باید یک صفحه مدیریتی حرفه‌ای باشد.

در بالای صفحه:

### Header

* عنوان: «موکل‌ها»
* توضیح کوتاه و کاربردی
* دکمه اصلی: «افزودن موکل»

مثلاً مفهوم توضیح:

> اطلاعات تماس، پرونده‌ها و مدارک موکل‌های خود را مدیریت کنید.

از متن‌های طولانی و غیرضروری استفاده نکن.

---

# 5. Summary

در صورت مناسب بودن با UI فعلی، یک Summary کوچک در بالای صفحه نمایش بده.

مثلاً:

```text
کل موکل‌ها
موکل‌های دارای پرونده فعال
موکل‌های بدون پرونده
```

تعداد کارت‌ها را محدود نگه دار.

هدف Summary ارائه اطلاعات مفید است، نه پر کردن صفحه.

---

# 6. جستجوی موکل

کاربر باید بتواند به‌سرعت موکل موردنظر خود را پیدا کند.

Search باید حداقل بر اساس موارد زیر کار کند:

* نام و نام خانوادگی
* شماره موبایل
* ایمیل در صورت وجود

جستجو باید سریع و بدون Reload صفحه انجام شود.

---

# 7. فیلتر و مرتب‌سازی

در صورت مناسب بودن با حجم داده، امکان Filter فراهم کن.

حداقل Filter پیشنهادی:

### وضعیت پرونده

* همه
* دارای پرونده فعال
* بدون پرونده فعال

### مرتب‌سازی

* جدیدترین موکل
* قدیمی‌ترین موکل
* نام الفبایی

اگر Filter خاصی ارزش UX واقعی ندارد، آن را صرفاً برای زیاد کردن امکانات اضافه نکن.

---

# 8. نمایش لیست موکل‌ها

لیست باید اطلاعات کلیدی را نمایش دهد.

حداقل:

```text
نام موکل
شماره موبایل
ایمیل در صورت وجود
تعداد پرونده‌ها
تعداد ضمائم
آخرین بروزرسانی
عملیات
```

روش نمایش را با توجه به طراحی فعلی پروژه انتخاب کن:

* Table
* Card
* List

اما برای Desktop، اگر تعداد اطلاعات زیاد است، Table احتمالاً انتخاب مناسبی است.

در Mobile نباید جدول Desktop صرفاً به شکل فشرده و غیرقابل استفاده نمایش داده شود.

---

# 9. Avatar

در صورت سازگاری با UI:

اگر تصویر پروفایل وجود ندارد، Avatar با Initial نام موکل نمایش بده.

مثلاً:

```text
علی رضایی
AR
```

اما Avatar نباید باعث شلوغی UI شود.

---

# 10. Empty State

اگر هیچ موکلی وجود ندارد:

یک Empty State حرفه‌ای نمایش بده.

مثلاً مفهوم:

> هنوز موکلی اضافه نکرده‌اید.
> اولین موکل خود را ثبت کنید تا بتوانید اطلاعات و پرونده‌های او را مدیریت کنید.

CTA:

**افزودن موکل**

اگر جستجو نتیجه‌ای ندارد، Empty State متفاوتی نمایش بده:

> موکلی با این مشخصات پیدا نشد.

و امکان پاک کردن Search را ارائه کن.

---

# 11. افزودن موکل

با کلیک روی:

**افزودن موکل**

یک Dialog / Sheet / صفحه فرم مناسب باز شود.

روش مناسب را بر اساس UI موجود انتخاب کن.

فرم اولیه:

### اطلاعات اصلی

* نام و نام خانوادگی *
* شماره موبایل *
* ایمیل
* کد ملی در صورت نیاز
* توضیحات

فعلاً اطلاعات اضافی غیرضروری را به فرم اضافه نکن.

---

# 12. اعتبارسنجی فرم

Validation سمت Frontend داشته باش.

حداقل:

* نام الزامی باشد.
* شماره موبایل معتبر باشد.
* ایمیل در صورت وارد شدن معتبر باشد.
* داده‌های خالی یا نامعتبر ذخیره نشوند.

پیام خطاها باید فارسی، واضح و نزدیک به فیلد مربوطه باشند.

---

# 13. ویرایش موکل

هر موکل باید امکان ویرایش داشته باشد.

ویرایش باید همان داده اصلی Client را تغییر دهد.

اگر همان Client در چند Case استفاده شده است، تغییر اطلاعات عمومی Client باید در همه جا قابل مشاهده باشد.

این موضوع بسیار مهم است.

---

# 14. حذف موکل

امکان حذف موکل وجود داشته باشد.

اما حذف باید با Confirmation انجام شود.

مثلاً اگر موکل دارای پرونده است:

قبل از حذف، هشدار واضح نمایش بده.

نباید حذف یک موکل باعث حذف تصادفی پرونده‌های او شود.

### اصل مهم

**Client و Case دو موجودیت مستقل هستند.**

حذف Client نباید بدون تصمیم صریح سیستم باعث حذف Case شود.

اگر به یک تصمیم معماری درباره رفتار حذف رسیدی که در این دستور مشخص نشده، قبل از پیاده‌سازی آن را مطرح کن.

---

# 15. صفحه / پنل جزئیات موکل

با کلیک روی یک موکل، باید امکان مشاهده جزئیات او وجود داشته باشد.

مسیر پیشنهادی:

```text
/admin/clients/[clientId]
```

یا اگر با معماری UI فعلی بهتر است، می‌توان از Drawer / Sheet / Modal استفاده کرد.

اما اگر اطلاعات زیاد است، صفحه مستقل ترجیح داده می‌شود.

---

# 16. Header جزئیات موکل

نمایش:

* نام موکل
* شماره موبایل
* ایمیل در صورت وجود
* Avatar
* تعداد پرونده‌ها
* عملیات

عملیات:

* ویرایش
* حذف

---

# 17. بخش اطلاعات تماس

نمایش مرتب اطلاعات:

```text
نام
شماره موبایل
ایمیل
کد ملی در صورت وجود
توضیحات
```

از نمایش اطلاعات به شکل فرم در حالت View خودداری کن.

View Mode باید خوانا و حرفه‌ای باشد.

---

# 18. بخش پرونده‌های موکل

این بخش بسیار مهم است.

نمایش تمام پرونده‌های مرتبط با موکل:

```text
پرونده‌ها

پرونده شماره 1024
طلاق توافقی
فعال

پرونده شماره 984
پرونده ملکی
بسته‌شده
```

حداقل اطلاعات:

* عنوان پرونده
* شماره پرونده
* وضعیت
* آخرین بروزرسانی

با کلیک روی پرونده، کاربر باید به صفحه همان پرونده منتقل شود:

```text
/cases/[caseId]
```

---

# 19. بخش ضمائم موکل

موکل باید بتواند ضمائم مرتبط با خودش داشته باشد.

نمایش:

* نام فایل
* نوع فایل
* حجم
* تاریخ
* عملیات

امکانات:

* افزودن فایل
* حذف فایل
* مشاهده / دانلود در صورت امکان

---

# 20. تفاوت ضمیمه موکل و ضمیمه پرونده

این مفهوم را در معماری رعایت کن:

### ضمیمه پرونده

مدرکی که مشخصاً مربوط به یک پرونده است.

مثلاً:

```text
دادخواست
رأی دادگاه
قرارداد
```

### ضمیمه موکل

مدرکی که مربوط به خود شخص است.

مثلاً:

```text
مدرک شناسایی
اطلاعات تماس
مدرک عمومی
```

این دو را بدون دلیل با یکدیگر ترکیب نکن.

---

# 21. مدیریت Attachment در نسخه فعلی

در این فاز Storage واقعی نداریم.

بنابراین:

* Metadata فایل را مدیریت کن.
* UI آپلود را بساز.
* Drag & Drop در صورت مناسب بودن طراحی اضافه شود.
* Loading / Success / Error State داشته باشد.

اما فایل‌های حجیم را به‌صورت نامحدود در Local Storage ذخیره نکن.

اگر برای Prototype نیاز به Persist واقعی فایل‌ها باشد، ابتدا راهکار مناسب را پیشنهاد بده و منتظر تأیید بمان.

---

# 22. اتصال با ماژول پرونده‌ها

این یکی از مهم‌ترین الزامات فاز است.

اگر:

```text
Client A
```

در صفحه پرونده‌ها ایجاد شود، باید در:

```text
/clients
```

قابل مشاهده باشد.

اگر:

```text
Client A
```

در `/admin/clients` ویرایش شود، اطلاعات آن در پرونده‌های مرتبط نیز باید به‌روز شود.

اگر پرونده‌ای برای Client ایجاد شود:

```text
Client A
 └── Cases: 1
```

باید تعداد پرونده‌های او در صفحه Clients نیز به‌روز شود.

**هیچ Duplicate Data Store برای Clients ایجاد نکن.**

---

# 23. Local Storage Architecture

ساختار را مانند فاز قبلی نگه دار:

```text
UI
 ↓
Hook / Store
 ↓
Client Service
 ↓
Local Storage
```

Componentها نباید مستقیماً داده Client را از Local Storage بخوانند.

منطق:

* Create
* Update
* Delete
* Get
* Search

را در یک لایه مشخص قرار بده.

---

# 24. State Management

اگر پروژه از Zustand استفاده می‌کند، از همان معماری استفاده کن.

مثلاً مفهومی:

```text
Client Store

clients

addClient()
updateClient()
deleteClient()
getClient()
searchClients()
```

اما قبل از ساخت Store جدید بررسی کن آیا Store موجود در فاز پرونده‌ها قابل توسعه است یا خیر.

**Storeهای موازی برای یک داده ایجاد نکن.**

---

# 25. UI/UX

این بخش باید با همان کیفیت ماژول پرونده‌ها باشد.

طراحی باید:

* مدرن
* حرفه‌ای
* مینیمال
* مناسب SaaS
* مناسب داشبورد حقوقی
* فارسی
* RTL

باشد.

از:

* Dialog
* Sheet
* Dropdown
* Badge
* Tooltip
* Toast
* Skeleton
* Empty State
* Confirmation
* Responsive Layout

در صورت نیاز استفاده کن.

اما از اضافه کردن Component صرفاً برای زیبایی خودداری کن.

---

# 26. تجربه کاربری

کاربر باید بتواند در کمترین تعداد کلیک:

```text
موکل‌ها
   ↓
انتخاب موکل
   ↓
مشاهده اطلاعات
   ↓
مشاهده پرونده‌ها
   ↓
باز کردن پرونده
```

را انجام دهد.

همچنین:

```text
موکل‌ها
   ↓
افزودن موکل
   ↓
ثبت اطلاعات
   ↓
ذخیره
   ↓
نمایش در لیست
```

باید کاملاً روان باشد.

---

# 27. Responsive

صفحه باید در:

* Desktop
* Tablet
* Mobile

قابل استفاده باشد.

در Mobile:

* اطلاعات غیرضروری مخفی یا منتقل شوند.
* Actions داخل Menu قرار گیرند.
* Table در صورت نامناسب بودن به Card/List تبدیل شود.
* فرم‌ها کاملاً قابل استفاده باشند.

---

# 28. فارسی و RTL

تمام UI باید کاملاً فارسی و RTL باشد.

توجه ویژه به:

* Alignment
* فاصله‌ها
* Icon placement
* فرم‌ها
* جدول
* Dropdown
* Modal
* اعداد
* شماره موبایل
* پیام‌های خطا

داشته باش.

---

# 29. Loading / Error / Empty

تمام عملیات مهم باید Feedback مناسب داشته باشند.

مثلاً:

* ذخیره موکل → Loading → Success
* حذف موکل → Confirmation → Success
* دریافت اطلاعات → Skeleton
* Search بدون نتیجه → Empty State
* داده خراب → Error State

---

# 30. آینده‌پذیری

فعلاً این قابلیت‌ها را نساز:

* چند وکیل
* شرکت / دفتر حقوقی
* پنل موکل
* پیام‌رسان
* Notification
* قرارداد
* پرداخت از صفحه موکل
* AI
* Timeline پیشرفته
* امضای دیجیتال

اما ساختار را طوری طراحی کن که اضافه شدن آن‌ها در آینده UI فعلی را خراب نکند.

---

# 31. موارد مهم ولی قابل تعویق

این موارد فعلاً اختیاری هستند و فقط در صورت مناسب بودن Scope به مرحله بعد منتقل شوند:

* آدرس کامل
* اطلاعات تکمیلی شخصی
* دسته‌بندی موکل
* برچسب‌ها
* یادداشت خصوصی وکیل
* تاریخچه فعالیت
* تاریخچه ارتباطات
* Export اطلاعات
* Import موکل‌ها

بدون تأیید من این موارد را پیاده‌سازی نکن.

---

# 32. قوانین پیاده‌سازی

* ابتدا معماری فعلی پروژه را بررسی کن.
* از Componentهای موجود استفاده مجدد کن.
* از Patternهای ماژول Cases استفاده کن.
* داده Client را Duplicate نکن.
* TypeScript را جدی بگیر.
* از `any` غیرضروری استفاده نکن.
* منطق Local Storage از UI جدا باشد.
* Componentهای بسیار بزرگ نساز.
* کد تکراری ایجاد نکن.
* قابلیت خارج از Scope را خودسرانه اضافه نکن.
* UI را با معیار کیفیت یک محصول واقعی SaaS طراحی کن.

---

# 33. تقسیم مراحل اجرا

این فاز را به مراحل زیر تقسیم کن:

### Phase 3.1 — بررسی و هماهنگ‌سازی معماری

* بررسی ماژول Cases
* بررسی Client موجود
* بررسی Store / Service
* بررسی Local Storage
* مشخص کردن اینکه چه چیزهایی باید Reuse شوند

**در این مرحله هیچ تغییر بزرگی ایجاد نکن.**

در پایان گزارش بده و متوقف شو.

---

### Phase 3.2 — مدل و مدیریت Client

* Typeهای Client
* Store / Service
* Local Storage
* CRUD
* اتصال با داده‌های Cases

در پایان تست کن که داده‌ها بین Cases و Clients مشترک باشند.

سپس متوقف شو.

---

### Phase 3.3 — صفحه `/admin/clients`

* Sidebar
* Header
* Summary
* Search
* Filter
* List / Table
* Empty State
* Responsive

سپس متوقف شو.

---

### Phase 3.4 — افزودن و ویرایش موکل

* Form
* Validation
* Create
* Edit
* Toast
* Error Handling

سپس متوقف شو.

---

### Phase 3.5 — جزئیات موکل

* `/admin/clients/[clientId]`
* اطلاعات تماس
* پرونده‌ها
* ضمائم
* Actions

سپس متوقف شو.

---

### Phase 3.6 — Attachments

* UI آپلود
* Metadata
* Drag & Drop
* Delete
* Loading / Error / Success

سپس متوقف شو.

---

### Phase 3.7 — UX و Responsive

کل ماژول را از نظر:

* UX
* RTL
* فارسی
* Responsive
* Accessibility
* Consistency

بازبینی کن.

در این مرحله مشکلات را اصلاح کن.

سپس متوقف شو.

---

### Phase 3.8 — تست نهایی

حداقل این سناریوها را تست کن:

1. ورود به صفحه موکل‌ها.
2. Empty State.
3. افزودن موکل.
4. نمایش موکل در لیست.
5. جستجوی موکل.
6. فیلتر.
7. ویرایش موکل.
8. حذف موکل.
9. مشاهده جزئیات.
10. مشاهده پرونده‌های موکل.
11. ورود از موکل به پرونده.
12. افزودن ضمیمه در حد Prototype.
13. حذف ضمیمه.
14. Refresh صفحه.
15. حفظ داده‌ها در Local Storage.
16. مشاهده همان Client در ماژول Cases.
17. تغییر اطلاعات Client و مشاهده تغییرات در Cases.
18. بررسی Mobile و Desktop.

---

# 34. قانون توقف

**این قانون بسیار مهم است.**

بعد از هر Phase:

1. کارهای انجام‌شده را گزارش کن.
2. فایل‌های تغییرکرده را اعلام کن.
3. مشکلات احتمالی را اعلام کن.
4. تست‌هایی که انجام شده را اعلام کن.
5. اگر تصمیمی نیاز به تأیید دارد، آن را مشخص کن.
6. سپس کاملاً متوقف شو.

بدون تأیید من وارد Phase بعدی نشو.

اگر در حین پیاده‌سازی به تصمیمی رسیدی که در این دستور مشخص نشده و می‌تواند روی معماری یا UX تأثیر بگذارد، ابتدا آن را مطرح کن.

---

# هدف نهایی

در پایان این فاز باید یک ماژول حرفه‌ای «موکل‌ها» داشته باشیم که:

* فقط یک لیست ساده نباشد.
* موکل را به‌عنوان یک موجودیت مستقل مدیریت کند.
* با ماژول پرونده‌ها کاملاً یکپارچه باشد.
* اطلاعات را در Local Storage حفظ کند.
* CRUD کامل داشته باشد.
* جستجو و فیلتر داشته باشد.
* جزئیات موکل را نمایش دهد.
* پرونده‌های مرتبط را نمایش دهد.
* ضمائم را در سطح Prototype مدیریت کند.
* UI/UX حرفه‌ای و مدرن داشته باشد.
* کاملاً فارسی و RTL باشد.
* Responsive باشد.
* و در آینده بتواند بدون بازنویسی اساسی به Backend واقعی متصل شود.

**اکنون فقط Phase 3.1 را انجام بده و پس از اتمام آن متوقف شو و منتظر تأیید بمان.**
