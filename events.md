# Phase 4 — پیاده‌سازی فرانت‌اند ماژول «رویدادها»

## هدف

در این فاز باید ماژول **رویدادها** برای داشبورد وکیل طراحی و پیاده‌سازی شود.

هدف این ماژول مدیریت برنامه‌ها، جلسات، دادگاه‌ها، مهلت‌ها و سایر اتفاقات مهم کاری وکیل است.

این بخش نباید صرفاً یک Date Picker یا جدول ساده باشد.

باید یک **مرکز مدیریت زمان و رویدادهای کاری وکیل** باشد.

---

# 1. محدودیت‌های فعلی

همانند فازهای قبلی:

* Backend نداریم.
* Database نداریم.
* API واقعی نداریم.
* Prisma نداریم.
* احراز هویت سمت سرور نداریم.
* پرداخت واقعی نداریم.
* Notification واقعی نداریم.
* اتصال به Google Calendar یا Outlook نداریم.

داده‌ها فعلاً در Frontend و `localStorage` مدیریت شوند.

سیستم احراز هویت فعلی پروژه حفظ شود.

معماری باید برای اتصال Backend در آینده آماده باشد.

---

# 2. بررسی معماری قبل از شروع

قبل از تغییر کد:

* ساختار پروژه را بررسی کن.
* ماژول Cases را بررسی کن.
* ماژول Clients را بررسی کن.
* Store و Serviceهای موجود را بررسی کن.
* نحوه مدیریت Local Storage را بررسی کن.
* کامپوننت‌های تقویم موجود در پروژه را بررسی کن.
* اگر Calendar Component مناسبی در پروژه وجود دارد، قبل از نصب وابستگی جدید آن را بررسی کن.

در این مرحله فقط تحلیل کن.

هیچ تغییر عمده‌ای انجام نده.

گزارش بده و منتظر تأیید بمان.

---

# 3. مسیر

در Sidebar داشبورد گزینه:

**رویدادها**

اضافه شود.

مسیر:

```text
/admin/events
```

Icon باید با سایر گزینه‌های Sidebar هماهنگ باشد.

---

# 4. مفهوم Event

هر رویداد باید یک موجودیت مستقل باشد.

مدل مفهومی:

```text
Event
├── id
├── title
├── type
├── date
├── startTime
├── endTime
├── location
├── description
├── clientId?
├── caseId?
├── status
└── ...
```

در این فاز TypeScript Type مناسب ایجاد کن.

---

# 5. انواع رویداد

نسخه اولیه حداقل این انواع را داشته باشد:

* جلسه با موکل
* جلسه دادگاه
* جلسه آنلاین
* مهلت قانونی
* یادآوری
* سایر

نام‌ها و Labelهای نهایی باید کاملاً فارسی باشند.

از رنگ‌های بسیار زیاد برای انواع رویداد استفاده نکن.

---

# 6. ارتباط Event با Case و Client

رویداد می‌تواند به:

* یک پرونده
* یک موکل
* هر دو
* یا هیچ‌کدام

متصل باشد.

مثلاً:

```text
جلسه با موکل
Client: علی رضایی
Case: پرونده ملکی
```

یا:

```text
یادآوری
بدون پرونده
بدون موکل
```

اگر Case انتخاب شد و Client آن Case مشخص است، UX مناسب برای جلوگیری از انتخاب اشتباه Client طراحی کن.

---

# 7. صفحه اصلی

صفحه `/admin/events` باید شامل:

### Header

* عنوان «رویدادها»
* توضیح کوتاه
* دکمه «ایجاد رویداد»

### نمای کلی

اطلاعات مفید مانند:

* رویدادهای امروز
* رویدادهای این هفته
* رویدادهای پیش‌رو

از کارت‌های زیاد استفاده نکن.

---

# 8. تقویم

تقویم بخش اصلی صفحه باشد.

حداقل Viewها:

* روزانه
* هفتگی
* ماهانه

کاربر بتواند بین Viewها جابه‌جا شود.

رویدادها باید روی تقویم نمایش داده شوند.

نمایش باید در RTL به‌درستی کار کند.

---

# 9. نمای لیست

در کنار Calendar، یا به‌عنوان View جداگانه، List View داشته باش.

مثلاً:

```text
امروز

۱۰:۰۰
جلسه با علی رضایی
پرونده ملکی

۱۴:۳۰
دادگاه
پرونده کلاهبرداری

۱۷:۰۰
یادآوری تماس با موکل
```

لیست باید براساس زمان مرتب شود.

---

# 10. رویدادهای گذشته و آینده

رویدادهای گذشته و آینده را از هم قابل تشخیص کن.

مثلاً:

* آینده → حالت عادی
* امروز → تأکید بصری
* گذشته → ظاهر کم‌رنگ‌تر
* رویداد مهم / مهلت قانونی → تأکید مناسب

از رنگ قرمز برای همه موارد مهم استفاده نکن.

---

# 11. ایجاد رویداد

با کلیک روی:

**ایجاد رویداد**

فرم مناسب باز شود.

اطلاعات:

### اطلاعات اصلی

* عنوان *
* نوع رویداد *
* تاریخ *
* ساعت شروع *
* ساعت پایان / مدت
* مکان
* توضیحات

### ارتباط

* موکل
* پرونده

---

# 12. Validation

حداقل:

* عنوان الزامی
* نوع الزامی
* تاریخ الزامی
* زمان شروع الزامی
* زمان پایان نباید قبل از شروع باشد.

پیام‌های خطا کاملاً فارسی و واضح باشند.

---

# 13. ایجاد سریع

برای UX بهتر، بررسی کن آیا امکان ایجاد سریع Event از روی Calendar وجود دارد.

مثلاً کاربر روی:

```text
۱۴۰۵/۰۶/۱۲ - ساعت ۱۰
```

کلیک کند و فرم ایجاد رویداد با تاریخ و ساعت از قبل پر شود.

اگر این قابلیت با Calendar انتخاب‌شده سازگار است، پیاده‌سازی شود.

---

# 14. جزئیات Event

با کلیک روی رویداد:

یک Dialog / Sheet / صفحه مناسب نمایش داده شود.

نمایش:

* عنوان
* نوع
* تاریخ
* ساعت
* مدت
* مکان
* موکل
* پرونده
* توضیحات

Actions:

* ویرایش
* حذف

---

# 15. ویرایش

امکان ویرایش کامل رویداد وجود داشته باشد.

پس از ویرایش:

* Calendar به‌روزرسانی شود.
* List به‌روزرسانی شود.
* ارتباط Case و Client حفظ شود.
* Local Storage به‌روزرسانی شود.

---

# 16. حذف

حذف Event باید Confirmation داشته باشد.

پیام باید واضح باشد که این عملیات قابل بازگشت نیست.

بعد از حذف:

* Calendar
* List
* Summary

باید بلافاصله به‌روز شوند.

---

# 17. جستجو

در صورت مناسب بودن با UX:

جستجو بر اساس:

* عنوان
* نام موکل
* عنوان پرونده
* توضیحات

اضافه شود.

اگر جستجو در Calendar باعث پیچیدگی UX می‌شود، آن را در List View ارائه کن.

---

# 18. فیلتر

فیلترها:

### نوع رویداد

* همه
* جلسه
* دادگاه
* آنلاین
* مهلت قانونی
* یادآوری
* سایر

### وضعیت زمانی

* امروز
* آینده
* گذشته

### ارتباط

* پرونده
* موکل

فیلترها باید ساده باشند و UI را شلوغ نکنند.

---

# 19. رویدادهای مهم

رویدادهایی مثل:

**مهلت قانونی**

اهمیت بالاتری دارند.

برای آنها Visual Indicator مناسب ایجاد کن.

اما فعلاً:

* Reminder واقعی
* Notification واقعی
* SMS
* Email

پیاده‌سازی نکن.

---

# 20. اتصال به پرونده‌ها

در صفحه پرونده در آینده باید بتوان رویدادهای مرتبط را نمایش داد.

برای همین:

```text
Event.caseId
```

را در مدل در نظر بگیر.

اگر معماری Cases فعلی اجازه می‌دهد، در این فاز یک بخش کوچک:

**رویدادهای مرتبط**

نیز در صفحه پرونده اضافه کن.

اما اگر باعث بزرگ شدن Scope فاز می‌شود، فقط رابطه داده را ایجاد کن و UI آن را به فاز بعد منتقل کن.

---

# 21. اتصال به موکل‌ها

همین منطق برای Client:

```text
Event.clientId
```

در نظر گرفته شود.

در صفحه موکل در آینده بتوان رویدادهای مرتبط را مشاهده کرد.

اگر اضافه کردن UI آن در این فاز Scope را زیاد می‌کند، فقط معماری داده را آماده کن.

---

# 22. Local Storage

ساختار:

```text
UI
 ↓
Hook / Store
 ↓
Event Service
 ↓
Local Storage
```

Componentها مستقیماً با Local Storage کار نکنند.

Store / Service مسئول:

* Create
* Update
* Delete
* Get
* Search
* Filter

باشد.

---

# 23. State Management

قبل از ساخت Store جدید بررسی کن آیا Store موجود پروژه قابل استفاده یا توسعه است.

اگر Zustand استفاده می‌شود:

```text
Event Store

events

addEvent()
updateEvent()
deleteEvent()
getEvent()
```

در صورت نیاز:

```text
getEventsByDate()
getEventsByCase()
getEventsByClient()
```

اضافه شود.

---

# 24. تاریخ و زمان

این قسمت بسیار مهم است.

از یک ساختار داده استاندارد برای ذخیره Date/Time استفاده کن.

نمایش تاریخ برای کاربر باید فارسی باشد.

تقویم و زمان‌بندی باید در محیط RTL درست عمل کند.

اگر کتابخانه تاریخ فعلی پروژه وجود دارد، ابتدا از همان استفاده کن.

بدون بررسی، وابستگی جدید اضافه نکن.

---

# 25. UI/UX

این بخش باید یکی از حرفه‌ای‌ترین صفحات داشبورد باشد.

هدف:

**کاربر با یک نگاه بفهمد امروز چه کارهایی دارد و چه چیزی در روزهای آینده منتظر اوست.**

از:

* Calendar
* List
* Badge
* Dialog
* Sheet
* Tooltip
* Toast
* Skeleton
* Empty State

در صورت نیاز استفاده کن.

طراحی نباید شبیه یک Calendar خام و عمومی باشد.

باید حس یک ابزار حرفه‌ای مدیریت کار وکیل را منتقل کند.

---

# 26. Empty State

اگر هیچ رویدادی وجود ندارد:

```text
امروز رویدادی ندارید.
```

یا اگر کل سیستم خالی است:

> هنوز رویدادی ثبت نکرده‌اید.
> جلسات، دادگاه‌ها و مهلت‌های مهم خود را ثبت کنید تا برنامه کاری شما همیشه مرتب باشد.

CTA:

**ایجاد رویداد**

---

# 27. Responsive

در Desktop:

Calendar می‌تواند بخش اصلی صفحه باشد.

در Mobile:

* Calendar باید قابل استفاده باشد.
* اطلاعات بیش از حد نمایش داده نشود.
* List View اهمیت بیشتری پیدا کند.
* Actions داخل Menu قرار بگیرند.
* فرم‌ها کاملاً قابل استفاده باشند.

---

# 28. فارسی و RTL

تمام UI:

* فارسی
* RTL
* سازگار با فونت پروژه
* دارای Alignment مناسب

باشد.

به‌خصوص:

* Calendar
* Date Picker
* Time Picker
* Dropdown
* Dialog
* فرم‌ها
* Navigation

را دقیق بررسی کن.

---

# 29. داده آزمایشی

برای تست UI، چند Event نمونه ایجاد کن:

* جلسه امروز
* جلسه آینده
* دادگاه
* مهلت قانونی
* رویداد گذشته

رویدادها را به Client و Caseهای موجود متصل کن.

داده‌های آزمایشی نباید داده واقعی Local Storage را خراب کنند.

---

# 30. قابلیت‌هایی که فعلاً نباید ساخته شوند

بدون تأیید من این موارد را اضافه نکن:

* Google Calendar
* Outlook
* Apple Calendar
* SMS
* Email Reminder
* Push Notification
* WhatsApp
* رزرو وقت توسط موکل
* تقویم چند وکیل
* تقویم شرکت
* Recurring Events پیشرفته
* Time Tracking
* مدیریت منابع / اتاق جلسه

این موارد می‌توانند در آینده بررسی شوند.

---

# 31. مراحل اجرا

## Phase 4.1 — بررسی معماری

* بررسی Cases
* بررسی Clients
* بررسی Date/Time libraries
* بررسی Calendar components
* بررسی Store/Service
* تعیین معماری Event

هیچ تغییر عمده‌ای انجام نده.

در پایان گزارش بده و متوقف شو.

---

## Phase 4.2 — مدل Event و Local Storage

* Type
* Store
* Service
* CRUD
* Local Storage
* ارتباط با Case
* ارتباط با Client

در پایان تست کن.

سپس متوقف شو.

---

## Phase 4.3 — صفحه اصلی Events

* Sidebar
* Header
* Summary
* Calendar
* List
* Navigation

سپس متوقف شو.

---

## Phase 4.4 — ایجاد و ویرایش Event

* Form
* Validation
* Create
* Edit
* Delete
* Confirmation

سپس متوقف شو.

---

## Phase 4.5 — Search / Filter

* Search
* Event Type
* Today / Future / Past
* Case / Client

سپس متوقف شو.

---

## Phase 4.6 — اتصال با Cases و Clients

اطمینان حاصل کن:

```text
Case
 ↓
Events

Client
 ↓
Events
```

در صورت مناسب بودن، نمایش رویدادهای مرتبط در صفحات Cases و Clients را نیز اضافه کن.

سپس متوقف شو.

---

## Phase 4.7 — UI/UX و Responsive

کل ماژول را از نظر:

* UX
* RTL
* فارسی
* Responsive
* Accessibility
* Visual consistency

بررسی و اصلاح کن.

سپس متوقف شو.

---

## Phase 4.8 — تست نهایی

حداقل این سناریوها را تست کن:

1. ورود به Events.
2. مشاهده Calendar.
3. مشاهده List.
4. ایجاد Event.
5. ایجاد Event از روی Calendar در صورت پیاده‌سازی.
6. ویرایش Event.
7. حذف Event.
8. Search.
9. Filter.
10. اتصال Event به Client.
11. اتصال Event به Case.
12. مشاهده Eventهای امروز.
13. مشاهده Eventهای آینده.
14. مشاهده Eventهای گذشته.
15. Refresh صفحه.
16. حفظ داده‌ها در Local Storage.
17. بررسی Desktop.
18. بررسی Mobile.
19. بررسی کامل RTL و تاریخ/زمان فارسی.

---

# 32. قانون توقف

بعد از هر Phase:

1. کارهای انجام‌شده را گزارش کن.
2. فایل‌های تغییرکرده را اعلام کن.
3. تست‌های انجام‌شده را اعلام کن.
4. مشکلات احتمالی را اعلام کن.
5. تصمیم‌های نیازمند تأیید را مشخص کن.
6. سپس متوقف شو.

بدون تأیید من وارد Phase بعدی نشو.

اگر در حین توسعه به قابلیت یا تصمیمی رسیدی که در این Master Prompt مشخص نشده و روی معماری، UX یا Scope اثر دارد، ابتدا آن را مطرح کن.

---

# هدف نهایی

در پایان این فاز باید یک ماژول «رویدادها» داشته باشیم که:

* فقط یک تقویم ساده نباشد.
* مرکز مدیریت زمان وکیل باشد.
* جلسه، دادگاه، مهلت قانونی و یادآوری را مدیریت کند.
* به پرونده و موکل متصل شود.
* Calendar و List View داشته باشد.
* Search و Filter داشته باشد.
* CRUD کامل داشته باشد.
* اطلاعات را در Local Storage حفظ کند.
* کاملاً فارسی و RTL باشد.
* UI/UX حرفه‌ای و مدرن داشته باشد.
* Responsive باشد.
* و در آینده بتواند به Backend و سیستم Notification واقعی متصل شود.

**اکنون فقط Phase 4.1 را اجرا کن و پس از اتمام آن کاملاً متوقف شو و منتظر تأیید بمان.**
