# Phase 5 — پیاده‌سازی فرانت‌اند ماژول «آمارها»

## هدف

در این فاز باید ماژول **آمارها** برای داشبورد وکیل طراحی و پیاده‌سازی شود.

هدف این ماژول ارائه یک نمای تحلیلی و قابل فهم از عملکرد کاری و کسب‌وکار وکیل در یک بازه زمانی مشخص است.

این بخش نباید صرفاً مجموعه‌ای از چند کارت آماری مانند:

```text
تعداد موکلین
تعداد پرونده‌ها
درآمد
```

باشد.

ماژول آمارها باید یک **مرکز تحلیل عملکرد وکیل** باشد که بتواند داده‌های موجود در سیستم را از بخش‌هایی مانند:

* Clients
* Cases
* Events
* Appointments
* Payments

جمع‌آوری و به شکل قابل فهم نمایش دهد.

هدف اصلی:

> وکیل بتواند با انتخاب یک بازه زمانی، در چند دقیقه متوجه شود چه اتفاقی در کسب‌وکار و فعالیت حرفه‌ای او افتاده است.

---

# 1. محدودیت‌های فعلی

همانند فازهای قبلی:

* Backend نداریم.
* Database نداریم.
* API واقعی نداریم.
* Prisma نداریم.
* احراز هویت سمت سرور نداریم.
* پرداخت واقعی نداریم.
* Notification واقعی نداریم.

داده‌ها فعلاً در Frontend و `localStorage` مدیریت می‌شوند.

سیستم احراز هویت فعلی پروژه حفظ شود.

ماژول Statistics نباید به API یا Backend فرضی وابسته شود.

معماری باید برای اتصال Backend در آینده آماده باشد.

---

# 2. بررسی معماری قبل از شروع

قبل از هر تغییر کد:

* ساختار پروژه را بررسی کن.
* Dashboard فعلی را بررسی کن.
* ماژول Cases را بررسی کن.
* ماژول Clients را بررسی کن.
* ماژول Events را بررسی کن.
* اگر Appointment / Booking وجود دارد، آن را بررسی کن.
* اگر Payment / Transaction وجود دارد، آن را بررسی کن.
* Storeهای موجود را بررسی کن.
* Serviceهای موجود را بررسی کن.
* نحوه ذخیره داده‌ها در Local Storage را بررسی کن.
* کتابخانه Chart موجود در پروژه را بررسی کن.
* اگر Chart Library مناسبی در پروژه وجود دارد، ابتدا از همان استفاده کن.
* قبل از نصب dependency جدید، وابستگی‌های موجود را بررسی کن.

در این مرحله فقط تحلیل کن.

هیچ تغییر عمده‌ای انجام نده.

در پایان گزارش بده و منتظر تأیید بمان.

---

# 3. مسیر

در Sidebar داشبورد گزینه:

**آمارها**

اضافه شود.

مسیر:

```text
/admin/statistics
```

Icon باید با سایر گزینه‌های Sidebar هماهنگ باشد.

---

# 4. مفهوم Statistics

Statistics یک Entity مستقل مانند Client یا Case نیست.

این ماژول باید یک **Analytics Layer** روی داده‌های موجود سیستم باشد.

معماری مفهومی:

```text
Clients
Cases
Events
Appointments
Payments
       ↓
Statistics / Analytics Service
       ↓
Aggregations
       ↓
Charts / KPI / Insights
```

Componentها نباید مستقیماً داده‌های خام را از Local Storage بخوانند.

---

# 5. انتخاب بازه زمانی

مهم‌ترین بخش این صفحه، انتخاب بازه زمانی است.

کاربر باید بتواند بازه مورد نظر خود را مشخص کند.

حداقل Presetها:

* امروز
* این هفته
* این ماه
* سه ماه اخیر
* امسال
* سال گذشته
* بازه دلخواه

همچنین امکان انتخاب:

**از تاریخ**

تا

**تا تاریخ**

وجود داشته باشد.

تمام آمار صفحه باید بر اساس بازه انتخاب‌شده محاسبه شوند.

---

# 6. رفتار Date Range

وقتی کاربر بازه را تغییر می‌دهد:

* KPIها به‌روزرسانی شوند.
* Chartها به‌روزرسانی شوند.
* جدول‌ها به‌روزرسانی شوند.
* مقایسه با دوره قبل در صورت فعال بودن محاسبه شود.
* Loading / Transition مناسب در نظر گرفته شود.

اگر داده‌ای برای بازه انتخاب‌شده وجود نداشته باشد، صفحه نباید خراب شود.

Empty State مناسب نمایش داده شود.

---

# 7. مقایسه با دوره قبل

در صورت امکان، آمارهای مهم با دوره قبلی مقایسه شوند.

مثلاً اگر کاربر انتخاب کرده:

```text
۱ شهریور تا ۳۱ شهریور
```

سیستم بتواند آن را با:

```text
۱ مرداد تا ۳۱ مرداد
```

مقایسه کند.

مثال:

```text
موکلین جدید

24

↑ 18%

نسبت به دوره قبل
```

برای هر KPI در صورت وجود داده کافی:

* مقدار فعلی
* مقدار دوره قبل
* درصد تغییر
* جهت تغییر

نمایش داده شود.

فرمول درصد تغییر:

```text
((current - previous) / previous) * 100
```

اگر مقدار دوره قبل صفر باشد، از نمایش درصد تغییر نامعتبر خودداری کن و UX مناسب ارائه بده.

---

# 8. KPIهای اصلی

در ابتدای صفحه چند KPI مهم نمایش داده شود.

از تعداد زیاد Card استفاده نکن.

حداقل KPIهای پیشنهادی:

### موکلین جدید

تعداد Clientهایی که در بازه انتخاب‌شده ایجاد شده‌اند.

### پرونده‌های ایجادشده

تعداد Caseهایی که در بازه انتخاب‌شده ایجاد شده‌اند.

### پرونده‌های بسته‌شده

تعداد پرونده‌هایی که در بازه انتخاب‌شده به وضعیت بسته‌شده رسیده‌اند.

### جلسات

تعداد Appointment / Eventهای مرتبط با جلسات در بازه.

### درآمد

در صورت وجود Payment Data معتبر، مجموع پرداخت‌های موفق در بازه.

### میانگین درآمد هر جلسه

در صورت وجود داده کافی:

```text
Total Revenue / Completed Paid Appointments
```

این KPIها باید بر اساس داده واقعی موجود در سیستم محاسبه شوند.

اگر یکی از Data Sourceها هنوز در پروژه وجود ندارد، KPI جعلی تولید نکن.

---

# 9. درآمد

اگر سیستم Payment در پروژه وجود دارد، بخش تحلیل درآمد طراحی شود.

حداقل اطلاعات:

* درآمد کل
* تعداد تراکنش‌های موفق
* میانگین مبلغ تراکنش
* پرداخت‌های ناموفق
* Refund در صورت وجود داده Refund

Revenue فقط از Paymentهای موفق محاسبه شود.

Paymentهای:

* Pending
* Failed
* Cancelled

نباید به عنوان درآمد قطعی محاسبه شوند.

---

# 10. روند درآمد

یک Chart برای نمایش روند درآمد ایجاد شود.

مثلاً:

```text
Revenue
│
│        ╭──╮
│   ╭────╯  ╰──╮
│───╯          ╰──
└──────────────────
```

محور زمان باید بر اساس بازه انتخاب‌شده Dynamic باشد.

مثلاً:

### یک روز

نمایش ساعتی

### یک هفته

نمایش روزانه

### یک ماه

نمایش روزانه یا هفتگی

### چند ماه

نمایش ماهانه

### یک سال

نمایش ماهانه

از ایجاد Chart بیش از حد پیچیده خودداری کن.

---

# 11. روند ایجاد پرونده

یک Chart برای نمایش تعداد پرونده‌های ایجادشده در طول زمان داشته باش.

مثلاً:

```text
پرونده‌های جدید
```

محور X:

زمان

محور Y:

تعداد پرونده

هدف این Chart:

مشاهده روند رشد یا کاهش Caseها.

---

# 12. وضعیت پرونده‌ها

یک بخش برای مشاهده توزیع وضعیت پرونده‌ها داشته باش.

مثلاً:

```text
پرونده‌ها

فعال        42
در انتظار    12
بسته‌شده     28
```

اگر پروژه Case Statusهای متفاوتی دارد، از همان Statusهای موجود استفاده کن.

Status جدید فقط برای نمایش Statistics ایجاد نکن.

Visualization می‌تواند:

* Donut Chart
* Progress Bars
* Segmented Visualization

باشد.

بهترین گزینه را با توجه به UI فعلی انتخاب کن.

---

# 13. وضعیت موکلین

در صورت وجود اطلاعات کافی، آمار Clientها نمایش داده شود.

مثلاً:

* موکلین جدید
* موکلین فعال
* موکلین دارای پرونده
* موکلین بدون پرونده

اما این موارد فقط زمانی نمایش داده شوند که مدل Client فعلی پروژه اطلاعات لازم را داشته باشد.

از ساخت Data Model مصنوعی صرفاً برای پر کردن Dashboard خودداری کن.

---

# 14. تحلیل جلسات

اگر Appointment یا Eventهای مناسب وجود دارند، بخش جلسات ایجاد شود.

آمارهای پیشنهادی:

* تعداد جلسات
* جلسات انجام‌شده
* جلسات لغوشده
* جلسات آینده
* جلسات بدون حضور در صورت وجود Status مناسب

همچنین در صورت وجود داده:

```text
Completion Rate
```

محاسبه شود.

مثلاً:

```text
84%

نرخ برگزاری جلسات
```

---

# 15. محبوب‌ترین خدمات

اگر Service Model در پروژه وجود دارد، آمار خدمات ارائه‌شده نمایش داده شود.

مثلاً:

```text
خدمات محبوب

مشاوره حقوقی       42
بررسی قرارداد      28
پرونده ملکی        17
```

هدف:

وکیل بفهمد چه خدماتی بیشتر مورد استفاده قرار گرفته‌اند.

اگر Service Model وجود ندارد، این بخش را فعلاً پیاده‌سازی نکن.

---

# 16. تحلیل نوع پرونده‌ها

اگر Caseها دارای Category / Type هستند، توزیع آنها نمایش داده شود.

مثلاً:

```text
انواع پرونده

ملکی       35%
خانواده    25%
کیفری      20%
قرارداد    20%
```

این بخش باید از Categoryهای واقعی سیستم استفاده کند.

---

# 17. رویدادها و فعالیت‌ها

در صورت وجود Event Data:

آمار رویدادها در بازه انتخاب‌شده نمایش داده شود.

مثلاً:

* تعداد کل رویدادها
* جلسات
* دادگاه‌ها
* مهلت‌های قانونی
* یادآوری‌ها

هدف این بخش بررسی حجم فعالیت کاری وکیل است.

---

# 18. مهلت‌های قانونی

اگر Event Type مربوط به مهلت قانونی وجود دارد:

یک شاخص مهم برای آن نمایش داده شود.

مثلاً:

```text
مهلت‌های قانونی

8
```

و در صورت وجود Status مناسب:

```text
گذشته
امروز
آینده
```

تفکیک شوند.

این بخش باید از نظر UX اهمیت بیشتری نسبت به Eventهای معمولی داشته باشد.

اما:

* Notification واقعی
* SMS
* Email
* Reminder واقعی

در این فاز ساخته نشوند.

---

# 19. نمودار فعالیت کاری

در صورت وجود داده کافی، یک نمای کلی از فعالیت وکیل در طول زمان ارائه شود.

مثلاً:

```text
فعالیت کاری

Mon  ███████
Tue  █████
Wed  █████████
Thu  ███
Fri  ██████
```

فعالیت می‌تواند بر اساس:

* جلسات
* پرونده‌های ایجادشده
* پرونده‌های بسته‌شده
* Eventها

محاسبه شود.

اما از ترکیب چند Metric متفاوت در یک نمودار بدون توضیح واضح خودداری کن.

اگر Visualization باعث ابهام می‌شود، آن را پیاده‌سازی نکن.

---

# 20. Insightهای ساده

صفحه فقط نمودار نباشد.

در صورت وجود داده کافی، چند Insight ساده و قابل فهم نمایش داده شود.

مثلاً:

> در این ماه ۱۸٪ موکل جدید بیشتری نسبت به ماه قبل داشته‌اید.

یا:

> بیشترین تعداد جلسات شما در روزهای شنبه ثبت شده است.

یا:

> درآمد این ماه نسبت به دوره قبل ۱۲٪ افزایش داشته است.

Insightها باید:

* مبتنی بر داده واقعی باشند.
* قابل محاسبه باشند.
* ادعای حقوقی یا مالی پیچیده نداشته باشند.
* در صورت نبود داده کافی نمایش داده نشوند.

در این فاز AI برای تولید Insight لازم نیست.

Insight Engine می‌تواند Rule-based باشد.

---

# 21. جدول جزئیات

در کنار Charts، در صورت نیاز یک بخش برای جزئیات ایجاد شود.

مثلاً:

### عملکرد روزانه

| تاریخ | جلسات | پرونده جدید | درآمد |
| ----- | ----: | ----------: | ----: |

جدول باید براساس بازه انتخاب‌شده Dynamic باشد.

اگر بازه بزرگ است، Pagination یا محدودسازی مناسب در نظر بگیر.

---

# 22. Drill Down

در صورت مناسب بودن UX، آمارهای قابل کلیک باشند.

مثلاً کاربر روی:

```text
24 موکل جدید
```

کلیک کند و بتواند به لیست Clientهای مرتبط منتقل شود.

یا:

```text
42 پرونده فعال
```

به Cases منتقل شود.

یا:

```text
€4,200 درآمد
```

به Transactions / Payments منتقل شود.

این Navigation باید در صورت وجود صفحه مقصد انجام شود.

اگر مقصد در پروژه وجود ندارد، لینک جعلی ایجاد نکن.

---

# 23. فیلترها

علاوه بر Date Range، در صورت وجود Data Model مناسب می‌توان فیلترهای محدود اضافه کرد.

مثلاً:

### نوع پرونده

همه

ملکی

خانواده

کیفری

...

### نوع خدمت

همه

مشاوره

بررسی قرارداد

...

### وضعیت پرونده

همه

فعال

بسته‌شده

...

فیلترها باید Contextual باشند.

همه فیلترها را همزمان در Header قرار نده.

هدف:

**صفحه تحلیلی ساده و قابل فهم باقی بماند.**

---

# 24. Export

در این فاز Export فقط در صورت وجود زیرساخت مناسب بررسی شود.

اگر پیاده‌سازی آن ساده و سازگار با معماری فعلی است:

* Export CSV

می‌تواند اضافه شود.

اما بدون تأیید:

* PDF Report Generator
* Excel پیچیده
* گزارش رسمی مالی
* گزارش حسابداری

پیاده‌سازی نشود.

---

# 25. Local Storage

Statistics نباید Storage جداگانه‌ای برای تمام Aggregationها ایجاد کند مگر اینکه معماری پروژه چنین نیازی داشته باشد.

ترجیح:

```text
Clients
Cases
Events
Payments
Appointments
      ↓
Statistics Service
      ↓
Computed Statistics
```

آمارها باید از داده‌های اصلی محاسبه شوند.

از ذخیره کردن آمارهای مشتق‌شده به شکلی که باعث Stale Data شود خودداری کن.

---

# 26. معماری

معماری پیشنهادی:

```text
UI
 ↓
Statistics Hook
 ↓
Statistics Service
 ↓
Domain Stores / Services
 ↓
Local Storage
```

Componentها نباید:

```text
localStorage.getItem(...)
```

را مستقیماً اجرا کنند.

Statistics Service مسئول:

* Date Range Filtering
* Aggregation
* Grouping
* Comparison
* Percentage Change
* KPI Calculation
* Chart Data Preparation
* Insight Calculation

باشد.

---

# 27. TypeScript

Typeهای مناسب برای Statistics ایجاد کن.

مثلاً در صورت نیاز:

```text
StatisticsDateRange
StatisticPeriod
KpiMetric
RevenueStatistics
CaseStatistics
ClientStatistics
AppointmentStatistics
EventStatistics
StatisticsComparison
StatisticsInsight
```

Typeها باید با Domain Modelهای واقعی پروژه هماهنگ باشند.

از `any` تا حد امکان استفاده نشود.

---

# 28. Performance

از آنجا که در آینده ممکن است تعداد داده‌ها زیاد شود:

* محاسبات غیرضروری در هر Render انجام نشود.
* Filtering و Aggregation تا حد امکان Memoize شود.
* داده‌ها چند بار از Storage خوانده نشوند.
* Chart Data فقط در صورت نیاز محاسبه شود.

اما در این فاز premature optimization انجام نده.

راهکار ساده و قابل فهم اولویت دارد.

---

# 29. Empty State

اگر هیچ داده‌ای وجود ندارد:

مثلاً:

> هنوز داده‌ای برای نمایش وجود ندارد.

و توضیح:

> با ثبت موکل، پرونده، جلسه و تراکنش، آمار عملکرد شما به‌صورت خودکار در این بخش نمایش داده می‌شود.

CTA مناسب در صورت وجود:

**ایجاد موکل**

یا

**ایجاد پرونده**

یا

**مشاهده رویدادها**

باشد.

اگر چند Data Source خالی هستند، یک Empty State جامع و غیرگیج‌کننده طراحی کن.

---

# 30. No Data در یک بخش

ممکن است کل سیستم داده داشته باشد ولی یک Metric داده نداشته باشد.

مثلاً:

```text
Revenue
```

داده ندارد ولی Cases دارد.

در این حالت کل صفحه را Empty نکن.

فقط همان Section:

> داده‌ای برای این بازه وجود ندارد.

نمایش دهد.

---

# 31. UI/UX

صفحه آمارها باید یکی از حرفه‌ای‌ترین صفحات Dashboard باشد.

هدف:

> وکیل با نگاه کردن به صفحه بتواند وضعیت کاری خود را درک کند، نه اینکه مجبور باشد تک‌تک نمودارها را تحلیل کند.

ساختار پیشنهادی:

```text
Header
│
├── عنوان آمارها
├── توضیح کوتاه
└── Date Range Picker
│
├── KPI Section
│
├── Revenue / Activity Overview
│
├── Cases / Clients
│
├── Appointments / Events
│
├── Distribution / Breakdown
│
└── Detailed Data
```

از Dashboard شلوغ خودداری کن.

---

# 32. سلسله‌مراتب اطلاعات

اطلاعات مهم‌تر باید زودتر دیده شوند.

ترتیب پیشنهادی:

### Level 1

KPIهای اصلی

### Level 2

روندها

### Level 3

Breakdown

### Level 4

جزئیات

### Level 5

Insightها / اطلاعات تکمیلی

کاربر نباید برای فهمیدن وضعیت کلی مجبور باشد تا پایین صفحه Scroll کند.

---

# 33. Chartها

از Chart بیش از حد استفاده نکن.

حداقل Visualizationهای پیشنهادی:

* Line Chart برای روند
* Bar Chart برای مقایسه
* Donut Chart برای Distribution
* Table برای جزئیات

هر Chart باید یک سؤال مشخص را پاسخ دهد.

مثلاً:

> درآمد من در طول این ماه چطور تغییر کرده؟

یا:

> پرونده‌های من بیشتر از چه نوعی هستند؟

اگر یک Chart سؤال مشخصی را پاسخ نمی‌دهد، حذف شود.

---

# 34. Tooltip

Chartها باید Tooltip مناسب داشته باشند.

اطلاعات Tooltip:

* تاریخ
* مقدار
* نوع Metric

کاملاً فارسی باشند.

---

# 35. Responsive

### Desktop

می‌توان چند Chart را کنار هم قرار داد.

### Tablet

Grid باید به شکل مناسب شکسته شود.

### Mobile

* KPIها Stack شوند.
* Chartها عرض کامل داشته باشند.
* Tableها Responsive باشند.
* Date Range Picker قابل استفاده باشد.
* اطلاعات غیرضروری مخفی یا Collapsible شوند.
* Horizontal Overflow کنترل شود.

صفحه نباید فقط Desktop Dashboard را کوچک کند.

---

# 36. فارسی و RTL

تمام UI:

* فارسی
* RTL
* سازگار با فونت پروژه

باشد.

به‌خصوص:

* Date Range Picker
* Charts
* Tooltip
* Legend
* Dropdown
* Table
* Numbers
* Labels

را بررسی کن.

جهت نمایش Chart از نظر بصری باید با UX فارسی سازگار باشد، اما داده‌های عددی و محورهای نمودار نباید به شکلی نمایش داده شوند که خوانایی آنها کاهش پیدا کند.

---

# 37. تاریخ و زمان

برای Filtering از ساختار Date/Time استاندارد پروژه استفاده کن.

نمایش برای کاربر:

```text
۱۴۰۵/۰۶/۰۱
```

یا فرمت مورد استفاده فعلی پروژه.

اگر پروژه از کتابخانه تاریخ مشخصی استفاده می‌کند، ابتدا همان را بررسی و استفاده کن.

بدون بررسی dependency جدید اضافه نکن.

---

# 38. اعداد و واحد پول

اعداد UI باید با استاندارد فعلی پروژه هماهنگ باشند.

مثلاً:

```text
۱٬۲۵۰٬۰۰۰ تومان
```

یا Currency تعریف‌شده سیستم.

واحد پول را Hardcode نکن اگر پروژه در آینده Multi-Currency شدن را محتمل می‌کند.

اما در این فاز Multi-Currency پیاده‌سازی نکن مگر اینکه از قبل در پروژه وجود داشته باشد.

---

# 39. Accessibility

بررسی کن:

* Contrast
* Keyboard Navigation
* Focus State
* Button Labels
* Tooltip Accessibility
* Screen Reader Labels
* Form Accessibility

Chartها نباید تنها راه دسترسی به اطلاعات باشند.

اگر یک داده مهم فقط داخل Chart قابل مشاهده است، یک Summary یا Table قابل دسترسی نیز در نظر بگیر.

---

# 40. قابلیت‌هایی که فعلاً نباید ساخته شوند

بدون تأیید من این موارد را اضافه نکن:

* AI Analytics
* Predictive Analytics
* Forecasting
* پیش‌بینی درآمد
* پیش‌بینی تعداد پرونده
* Business Intelligence پیشرفته
* Google Analytics Integration
* Meta Ads Analytics
* Marketing Attribution
* Multi-lawyer Analytics
* Multi-office Analytics
* Custom Report Builder
* Scheduled Reports
* Email Reports
* PDF Report Generator
* Excel Advanced Export
* Accounting Reports
* Tax Reports
* Real-time Analytics
* Backend Analytics Engine
* Data Warehouse
* BigQuery
* Event Tracking System جدید

این موارد می‌توانند در آینده بررسی شوند.

---

# 41. اصل مهم درباره داده‌های آماری

هر عددی که در این صفحه نمایش داده می‌شود باید قابل توضیح باشد.

اگر کاربر پرسید:

> «این عدد ۴۲ از کجا آمده؟»

معماری سیستم باید بتواند مشخص کند:

```text
42 = تعداد Caseهایی که:
createdAt
در Date Range انتخاب‌شده
قرار دارند.
```

از تولید اعداد نمایشی، Mock Statistic یا عددهای Hardcoded خودداری کن.

---

# 42. جلوگیری از داده جعلی

برای پر کردن نمودارها داده جعلی اضافه نکن.

اگر داده واقعی کافی نیست:

```text
No data
```

نمایش بده.

برای Development / UI Testing می‌توان از Seed Data موجود پروژه استفاده کرد، اما باید کاملاً از Production-like Data قابل تشخیص باشد.

---

# 43. اتصال با Dashboard اصلی

اگر Dashboard اصلی پروژه KPIهایی دارد که با Statistics مشترک هستند:

از منطق محاسباتی تکراری جلوگیری کن.

ترجیح:

```text
Shared Statistics / Analytics Service
```

که بتواند هم Dashboard و هم صفحه Statistics از آن استفاده کنند.

---

# 44. اتصال با سایر ماژول‌ها

ماژول Statistics باید بتواند در آینده روی این Data Sourceها توسعه پیدا کند:

```text
Clients
Cases
Events
Appointments
Payments
Services
```

اما فقط Data Sourceهایی را در این فاز پیاده‌سازی کن که واقعاً در پروژه وجود دارند.

---

# 45. مراحل اجرا

## Phase 5.1 — بررسی معماری

* بررسی Dashboard
* بررسی Clients
* بررسی Cases
* بررسی Events
* بررسی Appointments
* بررسی Payments
* بررسی Services
* بررسی Storeها
* بررسی Serviceها
* بررسی Date/Time
* بررسی Chart Library
* بررسی Local Storage
* تعیین Architecture برای Statistics

هیچ تغییر عمده‌ای انجام نده.

در پایان گزارش بده و متوقف شو.

---

## Phase 5.2 — Statistics Domain

* ایجاد Typeها
* ایجاد Statistics Service
* ایجاد Date Range Logic
* ایجاد Aggregation Logic
* ایجاد Comparison Logic
* ایجاد KPI Calculations

از داده واقعی موجود در پروژه استفاده کن.

در پایان تست کن.

سپس متوقف شو.

---

## Phase 5.3 — صفحه اصلی Statistics

* Sidebar
* Header
* Date Range
* KPI Section
* Layout اصلی
* Loading State
* Empty State

سپس متوقف شو.

---

## Phase 5.4 — Charts

بر اساس Data Sourceهای واقعی:

* Revenue Chart
* Cases Chart
* Client Chart
* Appointment/Event Chart
* Distribution Charts

فقط Chartهایی را پیاده‌سازی کن که داده واقعی برای آنها وجود دارد.

سپس متوقف شو.

---

## Phase 5.5 — Comparison و Insights

* Period Comparison
* Percentage Change
* Trend Indicators
* Rule-based Insights

در صورت وجود داده کافی.

سپس متوقف شو.

---

## Phase 5.6 — Details و Drill Down

* Detail Tables
* Relevant Navigation
* Case Drill Down
* Client Drill Down
* Payment Drill Down

فقط در صورت وجود صفحات مقصد.

سپس متوقف شو.

---

## Phase 5.7 — UI/UX

کل ماژول را از نظر:

* UX
* RTL
* فارسی
* Responsive
* Accessibility
* Visual Consistency
* Chart Readability
* Loading
* Empty State
* Error State

بررسی و اصلاح کن.

سپس متوقف شو.

---

## Phase 5.8 — تست نهایی

حداقل این سناریوها را تست کن:

1. ورود به Statistics.
2. نمایش صحیح صفحه.
3. انتخاب «امروز».
4. انتخاب «این هفته».
5. انتخاب «این ماه».
6. انتخاب «سه ماه اخیر».
7. انتخاب «امسال».
8. انتخاب بازه دلخواه.
9. تغییر Date Range و به‌روزرسانی KPIها.
10. مشاهده Comparison با دوره قبل.
11. بررسی حالت Previous Period = 0.
12. مشاهده Revenue در صورت وجود Payment Data.
13. مشاهده Case Statistics.
14. مشاهده Client Statistics.
15. مشاهده Appointment/Event Statistics.
16. مشاهده Chartها.
17. بررسی Tooltip.
18. بررسی Empty State.
19. بررسی No Data در یک Section.
20. بررسی Drill Down در صورت پیاده‌سازی.
21. Refresh صفحه.
22. حفظ داده‌های اصلی.
23. بررسی عدم ایجاد داده جعلی.
24. بررسی Desktop.
25. بررسی Tablet.
26. بررسی Mobile.
27. بررسی کامل RTL.
28. بررسی تاریخ فارسی.
29. بررسی اعداد و Currency.
30. بررسی Accessibility.

---

# 46. قانون توقف

بعد از هر Phase:

1. کارهای انجام‌شده را گزارش کن.
2. فایل‌های تغییرکرده را اعلام کن.
3. تست‌های انجام‌شده را اعلام کن.
4. مشکلات احتمالی را اعلام کن.
5. تصمیم‌های نیازمند تأیید را مشخص کن.
6. سپس متوقف شو.

بدون تأیید من وارد Phase بعدی نشو.

اگر در حین توسعه به قابلیت یا تصمیمی رسیدی که در این Master Prompt مشخص نشده و روی:

* Architecture
* UX
* Data Model
* Scope
* Performance

اثر دارد، ابتدا آن را مطرح کن.

---

# هدف نهایی

در پایان این فاز باید یک ماژول **«آمارها»** داشته باشیم که:

* فقط چند KPI ساده نباشد.
* امکان انتخاب بازه زمانی داشته باشد.
* عملکرد وکیل را در بازه مشخص تحلیل کند.
* با دوره قبل مقایسه داشته باشد.
* از داده‌های واقعی سیستم استفاده کند.
* اطلاعات Clients را تحلیل کند.
* اطلاعات Cases را تحلیل کند.
* Events / Appointments را تحلیل کند.
* در صورت وجود Payment Data، درآمد را تحلیل کند.
* Chartهای معنادار داشته باشد.
* اطلاعات را به شکل قابل فهم نمایش دهد.
* در صورت وجود داده کافی Insightهای ساده ارائه دهد.
* از داده جعلی استفاده نکند.
* Architecture مستقل و قابل توسعه داشته باشد.
* مستقیماً با Local Storage از داخل Componentها کار نکند.
* در آینده بتواند به Backend Analytics متصل شود.
* کاملاً فارسی و RTL باشد.
* Responsive باشد.
* Accessible باشد.
* UI/UX حرفه‌ای داشته باشد.

هدف اصلی:

> **وکیل با ورود به بخش آمارها باید بتواند در یک نگاه وضعیت کاری و عملکرد حرفه‌ای خود را در بازه زمانی انتخاب‌شده درک کند.**

---

# اکنون فقط Phase 5.1 را اجرا کن

در Phase 5.1 فقط:

* معماری پروژه را بررسی کن.
* Dashboard را بررسی کن.
* Clients را بررسی کن.
* Cases را بررسی کن.
* Events را بررسی کن.
* Appointments را بررسی کن.
* Payments را بررسی کن.
* Services را بررسی کن.
* Storeها و Serviceها را بررسی کن.
* Date/Time Library را بررسی کن.
* Chart Library را بررسی کن.
* Local Storage را بررسی کن.
* Data Sourceهای واقعاً موجود را مشخص کن.
* معماری پیشنهادی Statistics را ارائه کن.

**هیچ تغییر عمده‌ای در کد انجام نده.**

پس از پایان Phase 5.1:

1. خلاصه بررسی را گزارش کن.
2. Data Sourceهای موجود را اعلام کن.
3. Metricهایی که واقعاً قابل محاسبه هستند را مشخص کن.
4. Metricهایی که فعلاً قابل محاسبه نیستند را مشخص کن.
5. Architecture پیشنهادی را توضیح بده.
6. فایل‌هایی که در Phaseهای بعدی احتمالاً تغییر خواهند کرد را اعلام کن.
7. تصمیم‌های نیازمند تأیید را مشخص کن.
8. سپس کاملاً متوقف شو.

**بدون تأیید من وارد Phase 5.2 نشو.**
