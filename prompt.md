# Master Prompt — فارسی‌سازی و خلوت‌سازی پنل ادمین

تو یک **Senior Frontend Engineer و UI/UX Engineer** هستی. پروژه فعلی بر پایه‌ی `satnaing/shadcn-admin` ساخته شده و باید آن را برای استفاده در یک محصول کاملاً فارسی آماده کنی.

## قوانین کلی

- قبل از هر تغییر، ساختار فعلی پروژه و کامپوننت‌های مرتبط را بررسی کن و بر اساس معماری موجود تغییر بده؛ بازنویسی غیرضروری انجام نده.
- تغییرات را **فازبه‌فاز** انجام بده.
- در پایان هر فاز:
  1. تغییرات انجام‌شده را کوتاه گزارش کن.
  2. بررسی کن که پروژه Build/Typecheck/Lint مناسب داشته باشد و خطاهای ناشی از تغییرات را رفع کن.
  3. نتیجه‌ی قابل مشاهده‌ی همان فاز را توضیح بده.
  4. **متوقف شو و از من تأیید بگیر.**
- تا زمانی که من تأیید نکرده‌ام، وارد فاز بعدی نشو.
- از اضافه‌کردن قابلیت یا UI خارج از موارد خواسته‌شده خودداری کن.
- ساختار و قابلیت‌های مفید موجود را فقط در صورت نیاز تغییر بده.
- هدف، ساخت یک **اسکلت تمیز و مینیمال برای محصول فارسی** است، نه حفظ داشبورد دمو.

---

## فاز ۱ — بررسی و آماده‌سازی

ابتدا پروژه را بررسی کن و محل پیاده‌سازی موارد زیر را دقیق پیدا کن:

- Sidebar
- Sidebar Header / Logo
- Sidebar Collapse/Toggle
- Profile/User Menu
- Dashboard page
- Dashboard tabs
- نمودارها و داده‌های دمو
- Theme/Layout/Global CSS
- Font configuration

در این فاز هیچ تغییر ظاهری غیرضروری انجام نده. فقط ساختار را شناسایی و وابستگی‌های لازم را مشخص کن.

**پس از پایان فاز، متوقف شو و تأیید بگیر.**

---

## فاز ۲ — راست‌چین و فارسی‌سازی پایه

پروژه را به یک UI واقعی **RTL و فارسی** تبدیل کن:

- جهت کلی رابط کاربری `rtl` باشد.
- Layout، Sidebar، Header، Dropdown، Dialog و سایر اجزای مرتبط با RTL سازگار شوند.
- از فونت **Vazirmatn** به‌عنوان فونت اصلی فارسی استفاده کن.
- متن‌ها و Labelهای قابل مشاهده‌ی انگلیسی را با ترجمه‌ی روان و طبیعی فارسی جایگزین کن.
- ترجمه‌ها تحت‌اللفظی و ماشینی نباشند.
- متن‌های مربوط به UI، منوها، دکمه‌ها و پیام‌ها فارسی و یکدست باشند.
- RTL نباید باعث شکستن Layout یا Alignment شود.

**پس از پایان فاز، متوقف شو و تأیید بگیر.**

---

## فاز ۳ — خلوت‌سازی Sidebar

Sidebar را مطابق این ساختار تغییر بده:

### بخش اصلی Sidebar
از تمام آیتم‌های فعلی Sidebar فقط:

- **داشبورد**

باقی آیتم‌ها، گروه‌ها و لینک‌های اضافی حذف شوند.

### Profile در پایین Sidebar
دکمه Profile/User در پایین Sidebar باقی بماند.

با کلیک روی آن یک Dropdown باز شود که **فقط** شامل:

- **حساب کاربری**

باشد.

تمام گزینه‌های دیگر Profile Dropdown حذف شوند.

### Logo / Workspace Dropdown
در بالای Sidebar، بخش Logo/Workspace فعلی وجود دارد.

با کلیک روی آن Dropdown باز شود و **فقط** این دو گزینه را داشته باشد:

- **پنل کاربری**
- **پنل ادمین**

گزینه‌ی `Add Team` و هر قابلیت مربوط به اضافه‌کردن Team حذف شود.

**پس از پایان فاز، متوقف شو و تأیید بگیر.**

---

## فاز ۴ — خالی‌کردن Dashboard

صفحه‌ی اصلی Dashboard را کاملاً مینیمال کن.

- تمام نمودارها و ویجت‌های فعلی Dashboard حذف شوند.
- تمام داده‌های دمو و محتوای نمایشی غیرضروری حذف شوند.
- Tabهای بالای Dashboard حذف شوند.
- Dashboard فقط یک پیام خوش‌آمدگویی فارسی در مرکز صفحه داشته باشد، مثلاً:

**«خوش آمدید»**

پیام باید از نظر افقی و عمودی در فضای اصلی Dashboard به‌صورت تمیز و متعادل قرار بگیرد.

هدف این است که Dashboard فعلی فقط به یک **صفحه‌ی خالی و آماده برای توسعه محصول** تبدیل شود.

**پس از پایان فاز، متوقف شو و تأیید بگیر.**

---

## فاز ۵ — بازطراحی رفتار Collapse Sidebar

رفتار Sidebar در حالت Collapse را تغییر بده.

### حالت باز
Sidebar رفتار معمول خود را داشته باشد.

### حالت بسته
وقتی Sidebar بسته می‌شود:

- دکمه‌ی باز/بسته‌کردن Sidebar نباید کنار Sidebar قرار بگیرد.
- دکمه‌ی Toggle باید **داخل Sidebar و در بالاترین جایگاه آن** قرار بگیرد.
- این دکمه باید جایگاه Logo را در حالت بسته بگیرد.
- آیتم Dashboard در حالت بسته فقط Icon خود را نمایش دهد.
- Profile نیز رفتار مناسب حالت Collapse را حفظ کند.
- هیچ فضای خالی یا پرش ناگهانی در Layout ایجاد نشود.

### Animation
جابجایی بین حالت باز و بسته باید:

- نرم و روان باشد.
- بدون Flicker باشد.
- بدون تغییر ناگهانی عرض محتوا باشد.
- با Animation موجود پروژه هماهنگ باشد.
- در RTL نیز کاملاً درست عمل کند.

اگر برای این رفتار نیاز به تغییر ساختار Sidebar است، ابتدا معماری موجود را بررسی کن و کمترین تغییر لازم را انجام بده.

**پس از پایان فاز، متوقف شو و تأیید بگیر.**

---

## فاز ۶ — بررسی نهایی

در پایان کل پروژه را بررسی کن:

- RTL در تمام بخش‌های تغییر داده‌شده درست باشد.
- Vazirmatn به‌درستی اعمال شده باشد.
- هیچ متن انگلیسی غیرضروری در UI باقی نمانده باشد.
- Sidebar فقط Dashboard را داشته باشد.
- Profile Dropdown فقط «حساب کاربری» را داشته باشد.
- Logo Dropdown فقط «پنل کاربری» و «پنل ادمین» را داشته باشد.
- `Add Team` حذف شده باشد.
- Dashboard فقط پیام خوش‌آمدگویی داشته باشد.
- نمودارها و Tabهای قبلی حذف شده باشند.
- Collapse Sidebar رفتار جدید و Animation صحیح داشته باشد.
- Responsive بودن Layout حفظ شده باشد.
- خطاهای TypeScript/Lint/Build ناشی از تغییرات وجود نداشته باشد.

در پایان، خلاصه‌ی تغییرات نهایی را ارائه کن و اعلام کن که پروژه آماده‌ی توسعه قابلیت‌های اختصاصی محصول است.

**این فاز نیز پس از گزارش نهایی متوقف شود.**
