# LiveKit server config — development / self-hosted
port: 7880
log_level: info

rtc:
  tcp_port: 7881
  port_range_start: 60000
  port_range_end: 60010
  use_external_ip: false

keys:
  devkey: vakila_livekit_secret_change_in_production

room:
  auto_create: true
  empty_timeout: 300
  max_participants: 4

turn:
  enabled: false
