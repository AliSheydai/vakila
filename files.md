# Master Prompt — توسعه ماژول «پرونده‌ها»

## 1. نقش و مأموریت

تو به‌عنوان یک مهندس ارشد Full-Stack و طراح UI/UX محصول، مسئول توسعه ماژول «پرونده‌ها» در یک پلتفرم مدیریت وکلا هستی.

پروژه یک محصول Full-Stack است که با **Next.js** توسعه داده شده و در حال حاضر یک داشبورد اولیه و خام دارد.

هدف تو فقط اضافه‌کردن چند صفحه یا چند دکمه نیست.

باید این قابلیت را به‌صورت **حرفه‌ای، قابل توسعه، منسجم با معماری فعلی پروژه و آماده برای توسعه قابلیت‌های آینده** پیاده‌سازی کنی.

---

# 2. قانون بسیار مهم: قبل از تغییر کد

قبل از هرگونه تغییر در کد:

1. ساختار فعلی پروژه را بررسی کن.
2. معماری Frontend و Backend را بررسی کن.
3. سیستم احراز هویت و سطح دسترسی را بررسی کن.
4. ساختار فعلی داشبورد و Sidebar را بررسی کن.
5. کامپوننت‌های موجود و سیستم طراحی پروژه را شناسایی کن.
6. نحوه مدیریت State و درخواست‌های API را بررسی کن.
7. مدل‌های فعلی پایگاه داده و ارتباط موجود بین کاربران، وکلا و موکل‌ها را بررسی کن.
8. اگر برای این قابلیت زیرساختی از قبل وجود دارد، از آن استفاده کن و از ایجاد ساختار موازی خودداری کن.
9. قبل از پیاده‌سازی، وابستگی‌های لازم و تأثیر تغییرات بر بخش‌های موجود را مشخص کن.

**حق نداری بدون بررسی معماری فعلی، ساختار جدیدی را صرفاً از روی حدس ایجاد کنی.**

---

# 3. روش انجام کار

این پروژه باید کاملاً **مرحله‌ای** انجام شود.

تو نباید تمام قابلیت را یک‌باره پیاده‌سازی کنی.

پس از پایان هر مرحله:

* تغییرات انجام‌شده را بررسی کن.
* خطاهای TypeScript، Lint و Build را بررسی کن.
* نتیجه مرحله را به‌صورت خلاصه گزارش بده.
* مشکلات یا تصمیم‌های مهم را اعلام کن.
* سپس **متوقف شو و از من برای ادامه مرحله بعد تأیید بگیر.**

بدون تأیید من وارد مرحله بعد نشو.

---

# 4. ماژول اصلی: پرونده‌ها

در داشبورد یک گزینه جدید با عنوان:

**پرونده‌ها**

اضافه شود.

این بخش مرکز مدیریت پرونده‌های حقوقی وکلا خواهد بود.

ساختار مفهومی اولیه:

```text
پرونده‌ها
│
├── پرونده
│   ├── اطلاعات و شرح پرونده
│   └── ضمیمه‌ها / مدارک
│
├── تراکنش‌ها
│   ├── حق‌الزحمه
│   ├── پرداخت‌ها
│   └── هزینه‌ها
│
└── موکل‌ها
```

این ساختار فقط نقطه شروع است.

در صورت نیاز، ساختار را از نظر تجربه کاربری، مدل داده و معماری بهتر کن؛ اما بدون تغییر بی‌دلیل در محدوده محصول.

---

# 5. هدف اصلی ماژول

وکیل باید بتواند از یک نقطه واحد:

* پرونده‌های خود را مشاهده کند.
* پرونده جدید ایجاد کند.
* جزئیات هر پرونده را مشاهده کند.
* شرح و وضعیت پرونده را مدیریت کند.
* مدارک و فایل‌های مرتبط را مدیریت کند.
* موکل یا موکل‌های پرونده را مدیریت کند.
* حق‌الزحمه پرونده را مشخص کند.
* پرداخت‌های انجام‌شده را ثبت و مشاهده کند.
* هزینه‌های مربوط به پرونده را ثبت و مشاهده کند.
* وضعیت مالی پرونده را درک کند.

طراحی باید به‌گونه‌ای باشد که در آینده بتوان قابلیت‌هایی مانند:

* جلسات
* یادداشت‌ها
* وظایف
* مهلت‌های قانونی
* پیام‌ها
* قراردادها
* گزارش‌های مالی
* تاریخچه فعالیت‌ها

را بدون بازطراحی اساسی سیستم به آن اضافه کرد.

---

# 6. صفحه اصلی «پرونده‌ها»

صفحه اصلی ماژول پرونده‌ها باید یک نمای مدیریتی و کاربردی باشد، نه صرفاً یک جدول ساده.

حداقل باید امکان:

* مشاهده لیست پرونده‌ها
* جستجوی پرونده
* فیلتر پرونده‌ها
* مرتب‌سازی
* مشاهده وضعیت پرونده
* مشاهده موکل
* مشاهده تاریخ ایجاد / آخرین تغییر
* ایجاد پرونده جدید

وجود داشته باشد.

### وضعیت‌های پیشنهادی اولیه

سیستم باید از مفهوم وضعیت پرونده پشتیبانی کند.

مثلاً:

* جدید
* در حال بررسی
* فعال
* در انتظار اقدام
* بسته‌شده
* بایگانی‌شده

این وضعیت‌ها باید قابل توسعه باشند.

---

# 7. اطلاعات اصلی پرونده

هر پرونده باید یک شناسه و اطلاعات پایه مشخص داشته باشد.

حداقل ساختار مفهومی:

```text
شماره پرونده
عنوان پرونده
شرح کوتاه
نوع / حوزه حقوقی
وضعیت
تاریخ ایجاد
آخرین بروزرسانی
موکل / موکل‌ها
وکیل مسئول
```

در صورت وجود چند موکل یا چند وکیل، ساختار نباید به‌صورت تک‌مقداری و غیرقابل توسعه طراحی شود.

---

# 8. صفحه جزئیات پرونده

با ورود به یک پرونده، کاربر باید یک صفحه کامل و حرفه‌ای برای مدیریت آن پرونده مشاهده کند.

این صفحه باید از نظر UX به چند بخش واضح تقسیم شود.

پیشنهاد اولیه:

### سربرگ پرونده

نمایش:

* عنوان پرونده
* شماره پرونده
* وضعیت
* موکل
* تاریخ ایجاد
* آخرین بروزرسانی
* عملیات مهم

### بخش‌های اصلی

```text
اطلاعات پرونده
مدارک و ضمیمه‌ها
تراکنش‌های مالی
موکل‌ها
```

در صورت نیاز می‌توان برای UX بهتر از Tab استفاده کرد.

تصمیم نهایی درباره Tab، بخش‌بندی صفحه یا ترکیب هر دو را بر اساس حجم اطلاعات و استانداردهای UI/UX اتخاذ کن.

---

# 9. شرح پرونده

برای هر پرونده باید امکان ثبت و ویرایش شرح کامل پرونده وجود داشته باشد.

شرح پرونده باید برای اطلاعات نسبتاً طولانی مناسب باشد و تجربه کاربری خوبی برای وکیل ایجاد کند.

از یک Textarea ساده و کوچک برای این بخش استفاده نکن، مگر اینکه معماری فعلی پروژه دلیل مشخصی برای آن داشته باشد.

ساختار باید در آینده قابلیت توسعه به:

* یادداشت‌ها
* تاریخچه تغییرات
* یادداشت‌های داخلی
* اطلاعات تکمیلی

را داشته باشد.

---

# 10. ضمیمه‌ها و مدارک

هر پرونده باید بتواند فایل‌های مرتبط داشته باشد.

امکانات مورد انتظار:

* افزودن فایل
* مشاهده لیست فایل‌ها
* نمایش نام فایل
* نوع فایل
* حجم فایل
* تاریخ بارگذاری
* شخص بارگذاری‌کننده
* دانلود
* حذف در صورت داشتن دسترسی

رابط کاربری Upload باید حرفه‌ای باشد.

از Drag & Drop در صورت سازگاری با سیستم طراحی استفاده کن.

برای فایل‌ها وضعیت‌هایی مانند:

```text
در حال بارگذاری
موفق
ناموفق
```

را در نظر بگیر.

همچنین محدودیت‌ها و خطاهای مربوط به نوع و حجم فایل باید به‌صورت مناسب به کاربر نمایش داده شوند.

---

# 11. تراکنش‌های مالی

هر پرونده باید بتواند اطلاعات مالی مرتبط با خودش را داشته باشد.

تراکنش‌ها حداقل به سه دسته تقسیم شوند:

### حق‌الزحمه

مبلغ توافق‌شده برای خدمات حقوقی.

### پرداخت‌ها

مبالغی که موکل پرداخت کرده است.

### هزینه‌ها

هزینه‌هایی که در ارتباط با پرونده ایجاد شده‌اند.

---

# 12. نمای مالی پرونده

در بالای بخش مالی، یک خلاصه واضح ارائه شود.

برای مثال:

```text
حق‌الزحمه کل
پرداخت‌شده
باقی‌مانده
هزینه‌های پرونده
```

و در صورت نیاز:

```text
وضعیت پرداخت
```

مثلاً:

* پرداخت نشده
* بخشی پرداخت شده
* کامل پرداخت شده

این بخش باید به‌صورت بصری قابل فهم باشد و کاربر برای درک وضعیت مالی مجبور به محاسبه دستی نباشد.

---

# 13. مدیریت پرداخت‌ها

برای هر پرونده باید بتوان پرداخت‌ها را مشاهده و در صورت نیاز ثبت کرد.

اطلاعات پایه:

```text
مبلغ
تاریخ
روش پرداخت
وضعیت
توضیح
```

در صورت وجود سیستم پرداخت آنلاین در پروژه، معماری باید طوری باشد که پرداخت‌های آنلاین در آینده بتوانند به پرونده مرتبط شوند.

**از طراحی‌ای که فقط برای ورود دستی پرداخت‌ها مناسب باشد خودداری کن.**

---

# 14. مدیریت هزینه‌ها

وکیل باید بتواند هزینه‌های مرتبط با پرونده را ثبت کند.

مثلاً:

```text
هزینه دادرسی
هزینه کارشناسی
هزینه رفت‌وآمد
هزینه خدمات
سایر
```

هر هزینه حداقل باید شامل:

```text
عنوان
مبلغ
تاریخ
دسته‌بندی
توضیح
```

باشد.

---

# 15. مدیریت موکل‌ها

هر پرونده باید بتواند به یک یا چند موکل مرتبط شود.

موکل نباید صرفاً یک متن ذخیره‌شده در پرونده باشد.

باید به‌صورت یک موجودیت مستقل در سیستم در نظر گرفته شود تا در آینده بتوان:

```text
موکل
│
├── پرونده‌ها
├── پرداخت‌ها
├── مدارک
├── جلسات
├── پیام‌ها
└── اطلاعات تماس
```

را مدیریت کرد.

اگر سیستم فعلی مدل Client/Client را دارد، از همان استفاده کن.

اگر ندارد، قبل از طراحی مدل جدید، معماری مناسب را پیشنهاد بده.

---

# 16. تجربه کاربری و طراحی

این بخش بسیار مهم است.

ما نمی‌خواهیم یک داشبورد معمولی و شبیه پنل‌های آماده بسازیم.

طراحی باید:

* مدرن
* حرفه‌ای
* تمیز
* مینیمال
* مناسب یک محصول حقوقی حرفه‌ای
* خوانا
* سریع در استفاده
* دارای سلسله‌مراتب بصری واضح

باشد.

از کامپوننت‌های موجود پروژه و سیستم طراحی فعلی استفاده کن، اما اگر UI فعلی برای این قابلیت کافی نیست، آن را به‌صورت کنترل‌شده بهبود بده.

---

# 17. اصول UX

کاربر اصلی این بخش یک وکیل است.

بنابراین طراحی را برای استفاده روزمره و سریع در نظر بگیر.

از موارد زیر استفاده کن:

* وضعیت‌های بصری واضح
* فرم‌های مرحله‌بندی‌شده در صورت نیاز
* Dialog / Modal برای عملیات کوچک
* صفحه مستقل برای عملیات پیچیده
* فیلترهای کاربردی
* جستجوی سریع
* Empty State حرفه‌ای
* Loading State
* Skeleton
* Error State
* Success Feedback
* Confirmation برای عملیات خطرناک

از شلوغ‌کردن صفحه با کارت‌ها و عناصر تزئینی غیرضروری خودداری کن.

---

# 18. طراحی واکنش‌گرا

ماژول باید روی:

* دسکتاپ
* تبلت
* موبایل

قابل استفاده باشد.

اما اولویت اصلی برای پنل مدیریت وکیل، تجربه دسکتاپ است.

در موبایل، اطلاعات باید به شکل منطقی تغییر چیدمان دهند و جدول‌ها نباید باعث خراب‌شدن صفحه شوند.

---

# 19. تجربه خالی بودن اطلاعات

برای بخش‌هایی که هنوز اطلاعات ندارند، از صفحات خالی یا متن‌های ساده مانند:

> داده‌ای وجود ندارد

استفاده نکن.

برای مثال:

```text
هنوز پرونده‌ای ایجاد نکرده‌اید.

با ایجاد اولین پرونده، اطلاعات پرونده،
موکل، مدارک و وضعیت مالی آن را از همین بخش مدیریت کنید.

[ ایجاد پرونده ]
```

Empty State باید کاربر را به اقدام بعدی هدایت کند.

---

# 20. اعتبارسنجی و خطاها

تمام فرم‌ها باید اعتبارسنجی مناسب داشته باشند.

خطاها باید:

* واضح
* فارسی
* نزدیک به فیلد مربوطه
* قابل فهم برای کاربر غیرتکنیکی

باشند.

هیچ خطای خام Backend یا Error Stack نباید مستقیماً به کاربر نمایش داده شود.

---

# 21. امنیت و سطح دسترسی

پرونده‌های حقوقی شامل اطلاعات حساس هستند.

بنابراین امنیت باید از ابتدا در طراحی لحاظ شود.

حداقل:

* هر وکیل فقط پرونده‌های مجاز خودش را ببیند.
* دسترسی به فایل‌ها کنترل شود.
* APIها بدون بررسی مالکیت داده‌ها قابل استفاده نباشند.
* حذف اطلاعات نیازمند بررسی دسترسی باشد.
* شناسه پرونده به‌تنهایی نباید برای دسترسی به اطلاعات کافی باشد.

امنیت را فقط به رابط کاربری محدود نکن.

**محدودیت دسترسی باید در Backend نیز اعمال شود.**

---

# 22. پایگاه داده و Backend

قبل از ایجاد مدل‌های جدید:

1. مدل‌های فعلی را بررسی کن.
2. ارتباطات موجود را بررسی کن.
3. از ایجاد داده‌های تکراری جلوگیری کن.
4. روابط را به‌صورت منطقی و قابل توسعه طراحی کن.

ساختار باید حداقل امکان ارتباط بین این موجودیت‌ها را فراهم کند:

```text
Lawyer
   │
   ├── Cases
   │      │
   │      ├── Clients
   │      ├── Attachments
   │      ├── Fees
   │      ├── Payments
   │      └── Expenses
   │
   └── ...
```

نام‌گذاری، روابط، اعتبارسنجی و APIها باید با معماری فعلی پروژه هماهنگ باشند.

---

# 23. API و ارتباط Frontend / Backend

APIها باید:

* واضح
* قابل توسعه
* دارای اعتبارسنجی
* دارای کنترل دسترسی
* دارای پاسخ‌های قابل پیش‌بینی

باشند.

از ایجاد APIهای بیش از حد عمومی که کنترل دسترسی آنها دشوار است خودداری کن.

Frontend نباید منبع اصلی امنیت باشد.

---

# 24. عملکرد

در لیست پرونده‌ها، اگر تعداد پرونده‌ها زیاد شود، سیستم نباید تمام اطلاعات را یک‌جا دریافت کند.

در صورت نیاز از:

* Pagination
* جستجوی سمت سرور
* فیلتر سمت سرور
* مرتب‌سازی سمت سرور

استفاده کن.

در دریافت جزئیات پرونده نیز فقط داده‌های لازم را دریافت کن.

---

# 25. قابلیت توسعه آینده

این ماژول نباید طوری ساخته شود که اضافه‌کردن قابلیت‌های آینده نیازمند بازنویسی کامل باشد.

در آینده احتمال اضافه‌شدن این موارد وجود دارد:

```text
جلسات
یادداشت‌ها
وظایف
مهلت‌های قانونی
پیام‌ها
قراردادها
گزارش‌های مالی
تاریخچه فعالیت‌ها
هوش مصنوعی
```

بنابراین از طراحی‌های موقتی و غیرقابل توسعه خودداری کن.

---

# 26. اولویت‌بندی اجرای پروژه

پروژه را به فازهای زیر تقسیم کن:

## فاز ۱ — بررسی و طراحی فنی

* بررسی کامل پروژه
* بررسی معماری
* بررسی UI فعلی
* بررسی Database
* بررسی Authentication
* بررسی API
* شناسایی مدل‌های موجود
* طراحی پیشنهادی ماژول پرونده‌ها

در پایان این مرحله:

**هیچ کدی تغییر نده مگر در صورت نیاز به آزمایش بسیار محدود.**

گزارش بده و برای ادامه تأیید بگیر.

---

# Phase 2 — پیاده‌سازی فرانت‌اند ماژول «پرونده‌ها»

## هدف این فاز

در این فاز **هیچ Backend، Database، API واقعی یا احراز هویت سمت سرور ایجاد نکن.**

هدف فعلی فقط ساخت یک نسخه کامل، حرفه‌ای و قابل استفاده از ماژول «پرونده‌ها» در Frontend است.

تمام داده‌ها باید فعلاً در سمت Frontend مدیریت شوند و برای ماندگاری از `localStorage` استفاده شود.

این فاز یک **Prototype واقعی و قابل تعامل** است که بعداً باید بتوان Backend و Database واقعی را بدون بازنویسی کامل UI و منطق محصول به آن متصل کرد.

---

# 1. محدودیت‌های مهم این فاز

فعلاً موارد زیر **نباید ساخته شوند**:

* Backend
* Database
* Prisma
* API واقعی
* Route Handler برای داده‌ها
* Server Actions برای داده‌های پرونده
* کنترل دسترسی سمت سرور
* Storage واقعی فایل
* اتصال به سرویس خارجی
* پرداخت واقعی

همچنین فعلاً موارد زیر خارج از Scope هستند:

* چند وکیل در یک حساب
* شرکت یا دفتر حقوقی
* تیم و کارمند
* Organization
* Membership

ساختار فعلی را ساده نگه دار:

```text
User
  ↓
Cases
  ↓
Clients / Attachments / Financial Data
```

اما کد را به شکلی بنویس که در آینده بتوان Backend واقعی را جایگزین Local Storage کرد.

---

# 2. احراز هویت فعلی

پروژه در حال حاضر یک جریان احراز هویت فرانت‌اندی دارد که:

* شماره موبایل دریافت می‌کند.
* کد OTP دریافت می‌کند.
* نام کاربر را دریافت می‌کند.
* سپس کاربر را وارد داشبورد می‌کند.

**از همین سیستم موجود استفاده کن.**

آن را با سیستم جدید جایگزین نکن.

در این فاز احراز هویت فقط برای تجربه کاربری و شبیه‌سازی محصول است و امنیت واقعی ایجاد نمی‌کند.

اطلاعات Session / User فعلی را مطابق معماری موجود پروژه مدیریت کن.

اگر برای ذخیره وضعیت ورود از `localStorage` یا cookie فعلی پروژه استفاده شده است، همان الگو را حفظ کن.

---

# 3. معماری داده در Frontend

برای اینکه بعداً اتصال Backend راحت باشد، داده‌های پرونده را مستقیماً در Componentها پراکنده نکن.

یک لایه مشخص برای مدیریت داده ایجاد کن.

ساختار پیشنهادی:

```text
src/features/cases/
│
├── components/
├── hooks/
├── services/
├── types/
├── utils/
└── ...
```

در صورت سازگاری با معماری فعلی پروژه، از الگوی موجود `features/tasks` و `features/users` استفاده کن.

---

# 4. مدل داده Frontend

فعلاً مدل‌های زیر را در TypeScript تعریف کن:

```text
Case
Client
Attachment
Fee
Payment
Expense
```

رابطه مفهومی:

```text
Case
│
├── Client
│
├── Attachments
│
├── Fee
│
├── Payments
│
└── Expenses
```

### نکته مهم

اطلاعات Client را در هر Case به‌صورت متن تکراری ذخیره نکن.

Client باید یک موجودیت مستقل باشد.

به‌عنوان مثال:

```text
Client
├── id
├── name
├── phone
├── email
└── ...
```

و:

```text
Case
└── clientId
```

برای نسخه فعلی کافی است.

معماری را طوری طراحی کن که در آینده بتوان یک Client را به چند Case متصل کرد.

---

# 5. Local Storage

تمام اطلاعات مربوط به پرونده‌ها باید در `localStorage` ذخیره شوند تا بعد از Refresh صفحه از بین نروند.

برای جلوگیری از پراکنده‌شدن کلیدها، یک ساختار مشخص برای Storage داشته باش.

مثلاً:

```text
cases
clients
```

اما نام‌گذاری نهایی را با توجه به استانداردهای پروژه انتخاب کن.

### قوانین مهم

* Componentها نباید مستقیماً در چندین نقطه `localStorage.getItem()` و `setItem()` انجام دهند.
* یک لایه مشخص برای خواندن و نوشتن داده ایجاد کن.
* Parse و Serialize داده‌ها در همان لایه انجام شود.
* خطاهای مربوط به داده خراب مدیریت شوند.
* اگر `localStorage` خالی است، وضعیت اولیه مناسب ایجاد شود.

---

# 6. مدیریت State

پروژه در حال حاضر Zustand دارد.

اگر Zustand در معماری فعلی برای State مناسب استفاده شده، از همان استفاده کن.

اما:

**State مربوط به پرونده‌ها را در Componentهای مختلف پراکنده نکن.**

منطق اصلی باید در یک محل مشخص قرار بگیرد.

برای مثال:

```text
Case Store
│
├── cases
├── addCase()
├── updateCase()
├── deleteCase()
├── getCase()
└── ...
```

هر تغییری در State باید باعث به‌روزرسانی UI و Local Storage شود.

---

# 7. اضافه‌کردن «پرونده‌ها» به داشبورد

در Sidebar یک گزینه جدید اضافه کن:

**پرونده‌ها**

با Icon مناسب و هماهنگ با طراحی فعلی.

مسیر:

```text
/cases
```

با کلیک روی آن، صفحه مدیریت پرونده‌ها باز شود.

---

# 8. صفحه اصلی پرونده‌ها

صفحه `/cases` باید یک صفحه مدیریتی حرفه‌ای باشد.

این صفحه نباید فقط یک جدول ساده باشد.

حداقل شامل:

### Header

* عنوان «پرونده‌ها»
* توضیح کوتاه
* دکمه «ایجاد پرونده»

### Summary

در صورت مناسب بودن طراحی، چند شاخص مهم نمایش داده شود:

```text
کل پرونده‌ها
پرونده‌های فعال
در انتظار اقدام
بسته‌شده
```

از ایجاد کارت‌های زیاد و غیرضروری خودداری کن.

### لیست پرونده‌ها

اطلاعات مناسب برای هر پرونده:

* عنوان پرونده
* شماره پرونده
* موکل
* نوع پرونده
* وضعیت
* مبلغ حق‌الزحمه
* مبلغ پرداخت‌شده
* آخرین بروزرسانی
* عملیات

---

# 9. جستجو و فیلتر

کاربر باید بتواند پرونده‌ها را سریع پیدا کند.

حداقل:

### جستجو

جستجو بر اساس:

* عنوان پرونده
* شماره پرونده
* نام موکل

### فیلتر وضعیت

مثلاً:

* همه
* جدید
* در حال بررسی
* فعال
* در انتظار اقدام
* بسته‌شده
* بایگانی‌شده

### مرتب‌سازی

حداقل:

* جدیدترین
* قدیمی‌ترین
* آخرین بروزرسانی

منطق این موارد را در یک Component واحد و شلوغ قرار نده.

---

# 10. Empty State

اگر هیچ پرونده‌ای وجود ندارد، یک Empty State حرفه‌ای نمایش بده.

مثلاً مفهوم:

> هنوز پرونده‌ای ایجاد نکرده‌اید.
> اولین پرونده خود را ایجاد کنید تا اطلاعات پرونده، موکل، مدارک و وضعیت مالی آن را مدیریت کنید.

و یک CTA:

**ایجاد پرونده**

طراحی باید با هویت بصری کل داشبورد هماهنگ باشد.

---

# 11. ایجاد پرونده

با کلیک روی:

**ایجاد پرونده**

یک تجربه کاربری مناسب برای ایجاد پرونده باز شود.

برای فرم، حداقل اطلاعات:

```text
عنوان پرونده
شماره پرونده
نوع / حوزه حقوقی
موکل
شرح پرونده
وضعیت
```

موکل باید:

* از بین موکل‌های موجود انتخاب شود.
* یا در صورت نیاز از همان فرآیند یک موکل جدید ایجاد شود.

اگر فرم طولانی شد، از Step یا بخش‌بندی منطقی استفاده کن.

فرم نباید بیش از حد پیچیده شود.

---

# 12. صفحه جزئیات پرونده

مسیر:

```text
/cases/[caseId]
```

این صفحه باید مهم‌ترین صفحه این ماژول باشد.

ساختار پیشنهادی:

```text
Header پرونده

Tabs:
├── اطلاعات
├── مدارک
├── مالی
└── موکل
```

در صورت مشاهده اینکه Tab بهترین تجربه نیست، می‌توانی ساختار بهتری پیشنهاد بدهی؛ اما قبل از تغییر اساسی در معماری صفحه، دلیل آن را توضیح بده.

---

# 13. Header پرونده

در Header:

* عنوان پرونده
* شماره پرونده
* وضعیت
* نام موکل
* آخرین بروزرسانی
* دکمه ویرایش
* منوی عملیات

قرار بگیرد.

عملیات خطرناک مانند حذف باید از منوی جداگانه و با Confirmation انجام شود.

---

# 14. تب اطلاعات

نمایش:

### اطلاعات اصلی

* عنوان
* شماره
* نوع
* وضعیت
* تاریخ ایجاد
* آخرین بروزرسانی

### شرح پرونده

یک بخش مناسب برای نمایش و ویرایش شرح.

فعلاً **Rich Text Editor نساز**.

یک Textarea بزرگ و حرفه‌ای برای ویرایش کافی است.

---

# 15. تب موکل

نمایش اطلاعات موکل مرتبط:

* نام
* شماره موبایل
* ایمیل
* اطلاعات تکمیلی

امکان:

* ویرایش موکل
* تغییر موکل پرونده

وجود داشته باشد.

اگر معماری UI اجازه می‌دهد، امکان مشاهده پرونده‌های دیگر همان موکل را نیز در آینده بتوان اضافه کرد؛ اما فعلاً فقط در صورت نیاز واقعی پیاده‌سازی شود.

---

# 16. تب مدارک

در این نسخه، فایل‌ها فقط در سطح Prototype مدیریت می‌شوند.

امکانات UI:

* افزودن فایل
* Drag & Drop
* نمایش نام فایل
* نوع فایل
* حجم
* تاریخ
* حذف فایل
* دانلود در صورت امکان

### نکته مهم درباره Local Storage

فایل‌های واقعی را به‌صورت نامحدود در `localStorage` ذخیره نکن.

برای Prototype اگر نیاز به نمایش فایل داریم، راهکار مناسب انتخاب کن.

مثلاً:

* نگهداری Metadata در Local Storage
* استفاده از Object URL برای Session فعلی

یا در صورت نیاز به Persist واقعی فایل، قبل از انتخاب راهکار با من مطرح کن.

هدف این است که Browser با حجم زیاد فایل دچار مشکل نشود.

**هیچ File Storage واقعی در این فاز ایجاد نکن.**

---

# 17. تب مالی

این بخش یکی از مهم‌ترین قسمت‌های پرونده است.

بالای صفحه یک خلاصه مالی حرفه‌ای نمایش بده:

```text
حق‌الزحمه کل
پرداخت‌شده
باقی‌مانده
هزینه‌های پرونده
```

همچنین وضعیت پرداخت:

```text
پرداخت نشده
بخشی پرداخت شده
کامل پرداخت شده
```

---

# 18. حق‌الزحمه

امکان ثبت و ویرایش حق‌الزحمه وجود داشته باشد.

اطلاعات:

```text
مبلغ
توضیح
تاریخ / موعد در صورت نیاز
```

ساختار را طوری طراحی کن که در آینده بتوان پرداخت مرحله‌ای را اضافه کرد.

---

# 19. پرداخت‌ها

امکان افزودن پرداخت دستی وجود داشته باشد.

اطلاعات:

```text
مبلغ
تاریخ
روش پرداخت
توضیح
```

روش‌های اولیه می‌توانند شامل موارد مناسب و عمومی باشند.

پرداخت آنلاین واقعی در این فاز ساخته نمی‌شود.

اما مدل Frontend باید طوری باشد که بعداً بتوان Transaction واقعی را به آن متصل کرد.

---

# 20. هزینه‌ها

امکان ثبت هزینه مرتبط با پرونده:

```text
عنوان
دسته‌بندی
مبلغ
تاریخ
توضیح
```

نمونه دسته‌بندی:

* دادرسی
* کارشناسی
* رفت‌وآمد
* خدمات
* سایر

---

# 21. محاسبات مالی

مقادیر محاسباتی را ذخیره نکن مگر اینکه دلیل مشخصی وجود داشته باشد.

مثلاً:

```text
remaining =
totalFee - totalPaid
```

و:

```text
paymentStatus =
  unpaid
  partial
  paid
```

محاسبات باید از داده اصلی انجام شوند تا ناسازگاری ایجاد نشود.

---

# 22. مدیریت موکل‌ها

علاوه بر ارتباط موکل با پرونده، یک بخش مستقل برای مدیریت Clientها در نظر بگیر:

```text
Client
├── name
├── phone
├── email
└── cases
```

مسیر این بخش در صورت نیاز:

```text
/clients
```

اما اگر در فاز فعلی اضافه‌کردن صفحه مستقل Client باعث افزایش Scope می‌شود، ابتدا ارتباط Client با Case را پیاده‌سازی کن و صفحه مستقل را به مرحله بعد منتقل کن.

---

# 23. UI/UX

این قسمت بسیار مهم است.

طراحی باید شبیه یک **محصول واقعی SaaS حرفه‌ای برای وکلا** باشد.

نه یک CRUD ساده.

از:

* سلسله‌مراتب بصری مناسب
* فضای خالی مناسب
* Typography حرفه‌ای
* رنگ‌بندی کنترل‌شده
* Badgeهای وضعیت
* Tableهای خوانا
* Dialogهای مناسب
* Dropdown
* Tabs
* Toast
* Skeleton
* Empty State
* Error State
* Loading State

استفاده کن.

از شلوغ‌کردن UI با کارت‌های متعدد و عناصر تزئینی خودداری کن.

---

# 24. RTL و فارسی

تمام بخش‌های جدید باید کاملاً با RTL و فارسی هماهنگ باشند.

به موارد زیر توجه کن:

* اعداد
* تاریخ‌ها
* فاصله‌گذاری
* Alignment
* Icon placement
* فرم‌ها
* جدول‌ها
* پیام‌های خطا
* Empty State

هیچ متن انگلیسی غیرضروری در UI نهایی نمایش داده نشود.

---

# 25. Responsive

صفحات باید در:

* Desktop
* Tablet
* Mobile

قابل استفاده باشند.

اولویت پنل مدیریت دسکتاپ است، اما موبایل نباید Layout را خراب کند.

در جدول‌ها برای موبایل راهکار مناسب انتخاب کن؛ صرفاً با `overflow-x` مشکل را پنهان نکن مگر اینکه بهترین راهکار باشد.

---

# 26. Loading / Error / Success

برای تمام عملیات مهم حالت‌های مناسب داشته باش:

* Loading
* Success
* Error
* Empty

کاربر نباید بعد از کلیک روی یک دکمه بدون Feedback بماند.

---

# 27. حذف اطلاعات

برای حذف:

* پرونده
* موکل
* پرداخت
* هزینه
* فایل

از Confirmation مناسب استفاده کن.

برای عملیات خطرناک، متن Confirmation باید واضح باشد و مشخص کند چه چیزی حذف خواهد شد.

---

# 28. داده‌های آزمایشی

برای اینکه UI از همان ابتدا قابل بررسی باشد، اگر پروژه از Faker یا Seed Mock Data استفاده می‌کند، داده‌های نمونه مناسب برای پرونده‌ها ایجاد کن.

مثلاً چند پرونده با:

* وضعیت‌های مختلف
* موکل‌های مختلف
* وضعیت مالی متفاوت
* تعداد مدارک متفاوت

ایجاد کن.

اما این داده‌ها نباید باعث شوند بعداً داده واقعی Local Storage خراب شود.

---

# 29. سازگاری با Backend آینده

حتی اگر فعلاً Local Storage استفاده می‌کنیم:

منطق UI را مستقیماً به `localStorage` گره نزن.

ترجیحاً ساختار:

```text
UI
 ↓
Hook / Store
 ↓
Service
 ↓
Local Storage
```

داشته باش.

بعداً بتوانیم:

```text
UI
 ↓
Hook / Store
 ↓
Service
 ↓
API
```

را جایگزین کنیم.

این موضوع یکی از مهم‌ترین الزامات این فاز است.

---

# 30. استفاده از React Query

React Query و Axios در پروژه نصب و پیکربندی شده‌اند.

فعلاً چون API واقعی نداریم، برای همه چیز به‌زور React Query استفاده نکن.

اگر معماری موجود پروژه نیاز به آن ندارد، State و Service مناسب برای Local Storage استفاده کن.

اما ساختار Service را طوری طراحی کن که بعداً بتوان API واقعی را به React Query متصل کرد.

---

# 31. قوانین کدنویسی

* ابتدا معماری موجود پروژه را بررسی کن.
* از Patternهای موجود پروژه استفاده کن.
* از کامپوننت‌های موجود استفاده مجدد کن.
* Componentهای بسیار بزرگ نساز.
* منطق Local Storage را از UI جدا کن.
* TypeScript را جدی بگیر.
* از `any` بدون دلیل استفاده نکن.
* منطق محاسبات مالی را از UI جدا کن.
* کد تکراری ایجاد نکن.
* نام‌گذاری‌ها واضح و منسجم باشند.
* قابلیت‌های خارج از Scope را خودسرانه اضافه نکن.

---

# 32. مرحله‌بندی اجرای این فاز

این فاز را خودت به مراحل کوچک‌تر تقسیم کن.

پیشنهاد:

### Phase 2.1

ساخت Typeها، مدل داده Frontend و Service/Store مربوط به Local Storage.

### Phase 2.2

افزودن Sidebar + صفحه اصلی `/cases`.

### Phase 2.3

ساخت Create/Edit Case.

### Phase 2.4

ساخت صفحه `/cases/[caseId]`.

### Phase 2.5

ساخت بخش موکل و مدارک.

### Phase 2.6

ساخت بخش مالی.

### Phase 2.7

بازبینی کامل UI/UX و Responsive.

### Phase 2.8

تست کامل سناریوها و رفع مشکلات.

**بعد از هر مرحله متوقف شو و برای مرحله بعد از من تأیید بگیر.**

---

# 33. تست نهایی

در پایان فاز باید حداقل این سناریوها کار کنند:

1. ورود کاربر با سیستم Auth فعلی.
2. ورود به «پرونده‌ها».
3. مشاهده Empty State.
4. ایجاد پرونده.
5. مشاهده پرونده در لیست.
6. جستجوی پرونده.
7. فیلتر پرونده.
8. ویرایش پرونده.
9. مشاهده جزئیات.
10. ایجاد/انتخاب موکل.
11. افزودن مدارک در حد Prototype.
12. ثبت حق‌الزحمه.
13. ثبت پرداخت.
14. ثبت هزینه.
15. محاسبه صحیح باقی‌مانده.
16. تغییر وضعیت پرداخت.
17. حذف اطلاعات با Confirmation.
18. Refresh صفحه بدون از بین رفتن داده‌های ذخیره‌شده در Local Storage.
19. خروج و ورود مجدد با همان کاربر و حفظ داده‌های او در حد Prototype.
20. بررسی کامل Responsive.

---

# 34. قانون Scope

اگر هنگام پیاده‌سازی به قابلیت یا تصمیمی رسیدی که در این دستور مشخص نشده:

**خودسرانه پیاده‌سازی نکن.**

ابتدا:

1. نیاز را توضیح بده.
2. دلیل پیشنهاد را بگو.
3. تأثیر آن بر UX و معماری را توضیح بده.
4. پیشنهاد خودت را ارائه کن.
5. برای تأیید من صبر کن.

---

# هدف نهایی این فاز

در پایان این فاز باید یک نسخه Frontend واقعی و قابل استفاده از ماژول پرونده‌ها داشته باشیم که:

* کاملاً تعاملی باشد.
* اطلاعات را در Local Storage حفظ کند.
* احراز هویت فعلی پروژه را استفاده کند.
* UI/UX حرفه‌ای داشته باشد.
* فارسی و RTL باشد.
* روی موبایل و دسکتاپ قابل استفاده باشد.
* ساختار کد تمیز و قابل توسعه داشته باشد.
* و مهم‌تر از همه، بعداً بتوان Backend واقعی را بدون بازنویسی اساسی UI به آن متصل کرد.

**Phase 2.1 را شروع کن و پس از اتمام آن متوقف شو و گزارش بده.**


## فاز ۳ — ساخت صفحه اصلی پرونده‌ها

پیاده‌سازی:

* Sidebar item
* صفحه لیست پرونده‌ها
* جستجو
* فیلتر
* مرتب‌سازی
* وضعیت‌ها
* Empty State
* Loading State
* ایجاد پرونده

بعد از اتمام مرحله توقف کن.

---

## فاز ۴ — صفحه جزئیات پرونده

پیاده‌سازی:

* اطلاعات اصلی
* شرح پرونده
* ضمیمه‌ها
* موکل‌ها
* تراکنش‌ها

با طراحی حرفه‌ای و منسجم.

بعد از اتمام مرحله توقف کن.

---

## فاز ۵ — بخش مالی

پیاده‌سازی:

* حق‌الزحمه
* پرداخت‌ها
* هزینه‌ها
* خلاصه مالی
* وضعیت پرداخت

بعد از اتمام مرحله توقف کن.

---

## فاز ۶ — بهبود UI/UX

تمام بخش‌های ساخته‌شده را بازبینی کن.

موارد زیر را بررسی کن:

* یکپارچگی طراحی
* فاصله‌ها
* تایپوگرافی
* سلسله‌مراتب بصری
* فرم‌ها
* جدول‌ها
* فیلترها
* Empty State
* Loading
* Error
* Responsive
* Accessibility
* تجربه عملیات پرتکرار

اگر قسمتی از UI ضعیف است، آن را اصلاح کن.

هدف این مرحله این است که خروجی شبیه یک **محصول واقعی و حرفه‌ای** باشد، نه یک نمونه اولیه ساده.

---

## فاز ۷ — تست نهایی

سناریوهای اصلی را تست کن:

### سناریو ۱

ایجاد پرونده جدید

### سناریو ۲

افزودن موکل

### سناریو ۳

افزودن فایل

### سناریو ۴

ثبت حق‌الزحمه

### سناریو ۵

ثبت پرداخت

### سناریو ۶

ثبت هزینه

### سناریو ۷

مشاهده وضعیت مالی

### سناریو ۸

ویرایش پرونده

### سناریو ۹

حذف اطلاعات با کنترل دسترسی

### سناریو ۱۰

دسترسی وکیل به پرونده وکیل دیگر

سناریوی آخر **باید حتماً تست شود و نباید امکان دسترسی وجود داشته باشد.**

---

# 27. قوانین کلی توسعه

* قبل از تغییر، کد موجود را بخوان.
* کد موجود را بدون دلیل بازنویسی نکن.
* کامپوننت‌های تکراری ایجاد نکن.
* از کامپوننت‌های موجود استفاده کن.
* TypeScript را جدی بگیر.
* از `any` بدون دلیل استفاده نکن.
* منطق کسب‌وکار را در UI قرار نده.
* امنیت را به Frontend محدود نکن.
* اطلاعات حساس را در لاگ‌ها چاپ نکن.
* خطاها را مدیریت کن.
* Build باید در پایان موفق باشد.
* قابلیت‌های خارج از محدوده را خودسرانه اضافه نکن.

---

# 28. قانون تصمیم‌گیری UI/UX

در مواردی که درباره طراحی رابط کاربری جزئیات مشخص نشده است، خودت بر اساس اصول حرفه‌ای UI/UX تصمیم بگیر.

اما:

**زیبایی نباید باعث کاهش کاربردپذیری شود.**

اولویت:

```text
کاربردپذیری
   ↓
وضوح
   ↓
سرعت انجام کار
   ↓
یکپارچگی
   ↓
زیبایی
```

از طراحی‌های بیش از حد شلوغ، کارت‌های غیرضروری، انیمیشن‌های زیاد و عناصر تزئینی که ارزش عملی ندارند خودداری کن.

---

# 29. قانون بسیار مهم درباره Scope

اگر هنگام توسعه متوجه شدی قابلیتی برای کیفیت محصول ضروری است ولی در این دستورالعمل ذکر نشده:

آن را خودسرانه پیاده‌سازی نکن.

ابتدا:

1. دلیل نیاز را توضیح بده.
2. تأثیر آن بر معماری را بگو.
3. تأثیر آن بر زمان و پیچیدگی را بگو.
4. پیشنهاد خودت را ارائه بده.
5. برای تأیید من صبر کن.

---

# 30. فرمت گزارش پایان هر مرحله

در پایان هر مرحله دقیقاً این موارد را گزارش کن:

### مرحله انجام‌شده

توضیح کوتاه.

### تغییرات

فهرست فایل‌ها و بخش‌های تغییرکرده.

### تصمیم‌های مهم

تصمیم‌های معماری یا UI/UX.

### تست‌ها

تست‌ها و بررسی‌های انجام‌شده.

### مشکلات

مشکلات باقی‌مانده، در صورت وجود.

### مرحله بعد

توضیح کوتاه درباره مرحله بعد.

سپس متوقف شو و منتظر تأیید من بمان.

---

# هدف نهایی

هدف این پروژه ساخت یک «صفحه پرونده» ساده نیست.

هدف ساخت اولین بخش جدی از یک **سامانه حرفه‌ای مدیریت فعالیت وکلا** است.

این ماژول باید از همین ابتدا با دید محصولی طراحی شود؛ به شکلی که بعداً بتوان قابلیت‌های بیشتری مانند مدیریت جلسات، ارتباط با موکل، مدیریت اسناد، امور مالی، وظایف و قابلیت‌های هوش مصنوعی را بدون بازنویسی اساسی به آن اضافه کرد.

**قبل از هرگونه کدنویسی، فاز اول را اجرا کن و برای تأیید من متوقف شو.**
