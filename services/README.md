# Vokala services

Each subdirectory is an independent microservice.

| Service | Path | Role |
|---------|------|------|
| **app** | `services/app` | Next.js frontend / BFF |

Add new services beside `app` (e.g. `services/api`) and register them in the root `docker-compose.yml`.
